# Changelog

Release notes are published on [GitHub Releases](https://github.com/kagura-ai/memory-cloud/releases),
which is the canonical source for the complete release history. This file highlights the current
release train and preserves selected historical development notes.

## [v0.79.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.79.0) — 2026-09-25

Remote MCP boundaries and OAuth. Tool descriptions and server instructions describe what a context's stored text is instead of telling the model to follow it; the authorization server applies the scope, PKCE and resource rules its metadata advertises; `/mcp` checks an OAuth token's scope and audience; and a new script verifies the whole OAuth + MCP flow against a deployment.

### Added
- **Remote OAuth verification script** ([#1686](https://github.com/kagura-ai/memory-cloud/issues/1686)): `backend/scripts/verify_remote_oauth.py` checks discovery, DCR, S256 consent, token-endpoint negatives, refresh and revocation, `initialize` / `tools/list` / a read call in both MCP eras, and scope, audience and session handling against `--base-url`. The operator only performs the browser consent. Evidence is written as JSON and Markdown, with every token, code, verifier and session id recorded by presence and length only. See [Remote OAuth Verification](docs/ops/remote-oauth-verification.md).
- **Directory scope and data boundaries** ([#1682](https://github.com/kagura-ai/memory-cloud/issues/1682)): [docs/remote-mcp-directory-scope.md](docs/remote-mcp-directory-scope.md) describes the Remote MCP submission scope, where data comes from and goes, how stored notes are delivered, and how the Software Directory Policy clauses map onto the code. One clause (§1.F) is marked unresolved.
- **OAuth scopes on MCP tool calls** ([#1686](https://github.com/kagura-ai/memory-cloud/issues/1686)): with an OAuth access token, read-only tools (plus `recall` and `get_agent_bootstrap`) need `memory:read` and every other tool needs `memory:write`. A missing scope answers 403 with an `insufficient_scope` challenge. A token that names no `memory:*` scope uses its client's registered scope. API keys, agent-bound keys and session cookies are unchanged. See [OAuth scopes](docs/mcp-tools.md#oauth-scopes).

### Changed
- **Stored context text is described as data** ([#1682](https://github.com/kagura-ai/memory-cloud/issues/1682)): the `get_context_info`, `load_pinned`, `load_guardrails`, `get_agent_bootstrap` and `recall` descriptions, the static server instructions and the quick reference describe `usage_guide`, pinned memories and guardrails as notes written by context members, not instructions. `get_context_info.guardrails` gains a `provenance` label. `get_agent_bootstrap`'s `instructions` is now the static quick reference only; read `context.usage_guide` for the context's guide.
- **Authorization rules** ([#1686](https://github.com/kagura-ai/memory-cloud/issues/1686)):
  - PKCE accepts `S256` only, and a public client must send a `code_challenge`.
  - The granted scope is requested ∩ registered ∩ advertised, falling back to the client's registered scope when a request names no memory scope. `/register` stores the scopes the server defines, or the default scope when they include no memory scope.
  - A `resource` must name this server's MCP resource (any `/mcp` form) and is bound to the token as its audience.
  - A request that fails these rules gets an error page before consent.
  - DCR checks every `redirect_uris` entry; `claude.com` and `platform.openai.com` are accepted Claude and ChatGPT hosts.
  - An authorization code, a refresh token and a device code each yield one token, also under concurrent requests.
  - A repeated token-request parameter is refused.
  - See [Authorization Code Grant (MCP clients)](docs/api-reference.md#authorization-code-grant-mcp-clients).
- **`/mcp` authentication and sessions** ([#1686](https://github.com/kagura-ai/memory-cloud/issues/1686)): a token whose audience is not this server's MCP resource, or an invalid, expired or revoked token, answers 401 with `error="invalid_token"`. An unknown `Mcp-Session-Id` is continued for the caller; an id held by another user or workspace answers 404; `DELETE /mcp` ends the caller's session (204). See [Authentication and sessions on /mcp](docs/api-reference.md#authentication-and-sessions-on-mcp).

### Fixed
- **Authlib 1.8** ([#1686](https://github.com/kagura-ai/memory-cloud/issues/1686)): the OAuth server works with Authlib 1.8.0, which fresh installs now resolve.

### Migration
- **OAuth clients:** use PKCE `S256` (`plain` is refused), and send a `code_challenge` from a public client. Send `resource` as this server's MCP URL (any `/mcp` form), or leave it out. Send each token-request parameter once. At registration, every `redirect_uris` entry must be one the server accepts on its own.
- **MCP clients with OAuth tokens:** a token narrowed to `memory:read` gets `403 insufficient_scope` on write tools. Re-authorize requesting the `scope` the challenge names (it includes `memory:write`). Tokens issued through the default registration carry both scopes.
- **`get_agent_bootstrap`:** read `context.usage_guide` for the context's guide; `instructions` no longer starts with it.
- **Legacy SSE endpoints:** `GET /mcp/sse` and `POST /mcp/messages/…` answer `410` regardless of the session id; connect with Streamable HTTP on `/mcp`.

### Notes
- No database migration, no new environment variables.
- **Device flow:** a device code no longer resolves on `/device` after its token has been issued.
- **Plugins:** the Claude Code and Codex plugin manifests are bumped in lockstep.

## [v0.78.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.78.0) — 2026-09-25

MCP tool quality. Every tool now declares a title and the standard MCP annotations; a tool failure returns a stable error code, the next step and a correlation id instead of exception text; `reference` responses have a size budget and a way to page the rest; and `/device/authorize` accepts the form-encoded body RFC 8628 specifies.

### Added
- **Tool titles and annotations** ([#1683](https://github.com/kagura-ai/memory-cloud/issues/1683)): every tool in `tools/list` (every profile, both transports) carries a human-readable `title` and the standard `annotations` object with all four hints (`readOnlyHint`, `destructiveHint`, `idempotentHint`, `openWorldHint`): 31 read-only tools, 12 additive writes, 21 destructive tools, and `setup_connector` as the only open-world tool. `recall` and `get_agent_bootstrap` (with a `query`) are marked not read-only and destructive, because recall's Hebbian learning reweights and prunes graph edges; a client that confirms destructive tools will now ask before them. The legacy `readOnly` flag stays and is derived from `readOnlyHint`, so it is no longer sent on `recall` / `get_agent_bootstrap` and is now sent on `secret_get` / `secret_list`. The metadata adds about 160 characters per tool. See [Tool annotations](docs/mcp-tools.md#tool-annotations).
- **Selective, bounded `reference`** ([#1685](https://github.com/kagura-ai/memory-cloud/issues/1685)): new optional `fields` (`content`, `details`, `context`, `links`), `max_chars` (10,000–100,000 characters, default 20,000) and `content_offset` / `details_offset` / `context_offset`. A memory whose response fits comes back exactly as before. A larger one is bounded: `content` is sliced with `content_truncated` / `content_next_offset`, and a field that does not fit is left out with `<field>_omitted` and its size, never cut silently; following the offsets returns the complete data. A paged read counts as one access. REST `POST /api/v1/memory/reference` is unchanged.
- **Form-encoded `/device/authorize`** ([#1671](https://github.com/kagura-ai/memory-cloud/issues/1671)): `POST /api/v1/oauth/device/authorize` accepts `application/x-www-form-urlencoded` (`client_id`, optional `scope`, RFC 8628 §3.1) as well as JSON, so RFC-conformant device flows such as Hermes Agent's work. Every error from the endpoint now uses the RFC 6749 §5.2 body: an unknown client is 400 `invalid_client`; a missing `client_id`, a malformed body or an unsupported or missing `Content-Type` is 400 `invalid_request` (was 422 `VAL-001`); a body over 4,096 bytes is 413. The per-IP limit is counted before the body is read.
- **Official MCP Registry metadata** ([#1679](https://github.com/kagura-ai/memory-cloud/issues/1679)): `server.json` describes the Remote MCP server for the registry.

### Changed
- **Actionable MCP tool errors** ([#1684](https://github.com/kagura-ai/memory-cloud/issues/1684)): tool failures share one vocabulary. A refusal keeps its message and gains a stable code and a `help` line. A server failure returns `cause` (`timeout`, `service_unavailable` or `internal_error`), a fixed `message`, `help`, a `correlation_id` (the request's trace id) and retry advice: `retryable: true` for reads, and for a write whose outcome is unknown `retryable: false` with `outcome: "unknown"` and the read that shows whether it took effect. Exception text, stack traces and driver details stay in the server log under the same `correlation_id`. The JSON-RPC fallbacks of both transports follow the same classification. The server instructions returned by `get_context_info` / `get_agent_bootstrap` gain a short Errors section; the code table is in [Errors](docs/mcp-tools.md#errors).

### Fixed
- **CI MinIO image** ([#1691](https://github.com/kagura-ai/memory-cloud/issues/1691)): `minio/minio` is no longer pullable anonymously from Docker Hub or quay.io, so CI and the `minio` compose profile use Chainguard's MinIO build, pinned by digest. The compose healthcheck no longer needs `curl`, and the service runs as root so existing `minio_data` volumes stay writable.
- **SQLAlchemy pinned below 2.1** ([#1694](https://github.com/kagura-ai/memory-cloud/issues/1694)): SQLAlchemy 2.1.0 changes the default PostgreSQL driver for a bare `postgresql://` URL and its typing, which broke CI. `sqlalchemy>=2.0.0,<2.1` keeps installs and image builds on the 2.0 series; moving to 2.1 is [#1695](https://github.com/kagura-ai/memory-cloud/issues/1695).

### Notes
- No migration, no new environment variables.
- **MCP clients:** a client that displayed `error` from a tool failure should show `message`; one that matched on exception text should branch on `error` or `cause`. A tool guardrail with `on: "result"` that matched raw exception text should match the `error` code instead. Migration notes are in `docs/mcp-tools.md` › Errors.
- **SDKs:** `reference` on a memory whose response is over 20,000 characters may now return a `content` slice or omit a field; small memories are unchanged.
- **Plugins:** the Claude Code and Codex plugin manifests are bumped in lockstep.

## [v0.77.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.77.0) — 2026-09-24

Sign-in and client reach. The server can record which terms-of-service version each person accepted and ask again when it changes; three more native MCP clients can register over loopback DCR; `list_tags` gains the drill-down the SDKs already send; and `/kagura-memory:setup` understands the `kagura-mcp` query flags.

### Added
- **Server-side terms-of-service acceptance** ([#1665](https://github.com/kagura-ai/memory-cloud/issues/1665)): a new `TERMS_VERSION` setting (default empty = off). When set, `GET /api/v1/system/info` and `/auth/me` expose it; Google and GitHub sign-in carry the version the person agreed to (`accepted_terms`, bound to the OAuth state and consumed once), password login and MFA record it too, and each acceptance is stored in the append-only `terms_acceptances` table with a `terms.accepted` audit row. A new account is created only when the sign-up carries the current version; an invite sign-up without it goes back to `/join/<token>` with a notice. Existing users are never locked out: they sign in, then a blocking dialog asks them to accept the current version (`POST /api/v1/me/terms-acceptance`), also before a workspace invitation is accepted. See `docs/deployment.md`.
- **`with_tags` on MCP `list_tags`** ([#1669](https://github.com/kagura-ai/memory-cloud/issues/1669)): an array of up to 50 tags (200 characters each); only memories carrying all of them are counted, and those tags are left out of the result, so it lists the tags that co-occur with them — the same rules as REST `?with_tags=`. Before, an SDK that sent it got the unfiltered tag cloud without an error. The REST `GET /api/v1/contexts/{id}/tags` response gains an optional `context_name`.
- **Loopback DCR for Codex, Hermes Agent and OpenClaw** ([#1657](https://github.com/kagura-ai/memory-cloud/issues/1657)): `POST /api/v1/oauth/register` accepts an RFC 8252 loopback redirect whose `client_name` names Codex, Hermes Agent or OpenClaw, and stores `codex` / `hermes` / `openclaw` as the provider. The names are accepted on loopback redirects only; existing matches keep their provider. Hermes Agent's device flow still needs a form-encoded `/device/authorize` body, tracked in [#1671](https://github.com/kagura-ai/memory-cloud/issues/1671).

### Changed
- **Sign-in buttons wait for `/system/info`** ([#1665](https://github.com/kagura-ai/memory-cloud/issues/1665)): on `/login`, `/join/<token>` and `/invite/<token>` the provider buttons stay disabled until the first `/system/info` answer, so they know whether a terms version applies; a failed fetch unlocks them. This is the only visible change while `TERMS_VERSION` is empty.

### Fixed
- **`/kagura-memory:setup` and `kagura-mcp` query flags** ([#1670](https://github.com/kagura-ai/memory-cloud/issues/1670)): the skill computes a `kagura-mcp` entry's upstream URL the way the proxy does, with `--guardrails` and `--tool-profile` (kagura-mcp 0.39.0+) applied on top of `--server` or the profile URL, and reports where each query parameter came from. An entry with `--guardrails off` is reported as in the hooks lane. To apply the lane it offers `--guardrails off`, replacing an existing `--guardrails <context-id>`, keeps `--tool-profile` when it rebuilds an entry, and offers the `--server …?guardrails=off` pin only for an older CLI. `docs/mcp-clients.md`, `docs/troubleshooting.md` and the guide say the same.

### Notes
- **Migration:** `alembic upgrade head` (`e85_1665_terms_acceptances`) creates the new, empty `terms_acceptances` table.
- **New optional setting:** `TERMS_VERSION` (`[A-Za-z0-9._-]{1,64}`, default empty). Empty keeps today's behaviour. Setting or changing it asks every existing user on their next page load; nobody is signed out, and API keys and MCP clients are not affected.
- **Plugins:** the Claude Code and Codex plugin manifests are bumped in lockstep.

## [v0.76.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.76.0) — 2026-09-23

Device and MCP sign-in for new users. A closed-beta invite now survives a CLI device login and an MCP client's sign-in, and the two unauthenticated device-flow endpoints get per-address request limits and a cleanup job for expired codes.

### Added
- **Invites through device and MCP sign-in** ([#1655](https://github.com/kagura-ai/memory-cloud/issues/1655)): `/join/<token>` accepts an optional `return_to`, validated like the one on `/login`. A link such as `https://<host>/join/<token>?return_to=%2Fdevice%3Fuser_code%3D<code>` signs the invitee up with the invite and lands them on `/device` with the code filled in. When the deployment has beta invites on, `/login` shows an "I have an invite link" entry that takes a pasted `/join/<token>` link or a bare token and continues to `/join` with the page's `return_to`, so an MCP client's authorize request resumes after sign-up (same-origin deployments). A signed-in visitor on `/join` with a usable `return_to` is sent there ("Continue"); the expired, invalid and disabled screens keep `return_to` on their link back to `/login`. The invite token only ever travels as the `/join` path segment.
- **Hourly cleanup of expired device codes** ([#1656](https://github.com/kagura-ai/memory-cloud/issues/1656)): rows in `oauth_device_codes` whose `expires_at` is older than `OAUTH_DEVICE_CODE_RETENTION_SECONDS` (default 3600) are deleted; before, a code nobody approved stayed until its OAuth client was deleted.

### Changed
- **`/join/<token>` asks for terms acceptance** ([#1655](https://github.com/kagura-ai/memory-cloud/issues/1655)): the same checkbox as `/login`; both provider buttons stay disabled until it is ticked. Recording acceptance on the server is tracked in [#1665](https://github.com/kagura-ai/memory-cloud/issues/1665).
- **Per-address limits on `POST /api/v1/oauth/device/authorize` and `POST /api/v1/oauth/device/verify`** ([#1656](https://github.com/kagura-ai/memory-cloud/issues/1656)): 10 and 30 requests per minute per client address by default (`OAUTH_DEVICE_AUTHORIZE_RATE_LIMIT_PER_MINUTE`, `OAUTH_DEVICE_VERIFY_RATE_LIMIT_PER_MINUTE`). Over the limit, `device/authorize` answers **429** in the RFC 6749 error shape (`invalid_request`, `Cache-Control: no-store`) and `device/verify` answers **429** `HTTP-429`, both with `Retry-After: 60`; no row is written and no lookup runs. `device/verify` counts every call, found or not. A Redis outage lets requests through. The `/device` page shows a "too many attempts" message on a 429 instead of "invalid code".
- **Redis counters repair a missing expiry** ([#1656](https://github.com/kagura-ai/memory-cloud/issues/1656)): the shared request counter sets its TTL whenever the key has none, not only on the first increment, so a counter whose expiry was lost resets on the next call instead of never.

### Fixed
- **Device-flow paths in the API reference** ([#1656](https://github.com/kagura-ai/memory-cloud/issues/1656)): `docs/api-reference.md` named `/api/v1/oauth2/...` for the device authorize, token and revoke steps; they are `/api/v1/oauth/...`.

### Notes
- No migration. Three new optional settings with defaults: `OAUTH_DEVICE_AUTHORIZE_RATE_LIMIT_PER_MINUTE`, `OAUTH_DEVICE_VERIFY_RATE_LIMIT_PER_MINUTE`, `OAUTH_DEVICE_CODE_RETENTION_SECONDS`.
- **Behind a reverse proxy** the limits key on the client address only when the server trusts the proxy's forwarded headers (uvicorn `--forwarded-allow-ips` / `FORWARDED_ALLOW_IPS`); otherwise every caller shares the proxy's budget.
- **API clients:** a CLI that calls `device/authorize` should treat a 429 as retryable after `Retry-After`.
- **Plugins:** the Claude Code and Codex plugin manifests are bumped in lockstep.

## [v0.75.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.75.0) — 2026-09-23

Plan, flag and quota gates. The server now says *why* it refused — `details.gate` on every plan and quota refusal, REST and MCP alike — and which plan features it actually enforces; the web UI decides every gate through one descriptor, offers an upgrade only where the Plan page exists for the viewer, and translates the gate copy it had left in English. Also: a `/kagura-memory:setup` plugin command. Five refusals answer a different `error` code and two of them a different status — read **Changed** before upgrading an API client that branches on them.

### Added
- **Machine-readable plan and quota refusals** ([#1644](https://github.com/kagura-ai/memory-cloud/issues/1644)): every plan or quota refusal carries a `details` block whose `gate` says why — `plan` (a higher tier lifts it), `quota` (a cap was reached), `allowlist` (a rollout kill switch: plan-neutral, never an upgrade path) or `deployment` (the operator switched the feature off: never an upgrade path) — with `feature`, `required_plan` (the tier key to branch on) / `required_plan_display` (its label; both `null` when no tier lifts the refusal) and `current_plan`, and on a quota `quota_type` from a frozen vocabulary, integer `current` / `limit`, and `resets_at` where a window applies. Before, a plan refusal and a rollout kill switch were the same `403 FEAT-001` on the wire. The annotation is additive: no key is renamed or removed, and the legacy count fields (`used_today`, `owned_count` / `cap`, `max_connectors` / `active_connectors`, …) keep shipping beside `current` / `limit`. Only a **typed** quota refusal — one whose `quota_type` is in the vocabulary — carries `gate: "quota"`: the 1 MB memory-size guard, the total memory cap and the "workspace not found" anomalies stay `QUOTA-001` / 429 with no `gate`, because no tier lifts them. The BYOK embedding spend caps (`QUOTA-002`) and the daily API quotas carry `gate` and `quota_type` but no counts. Role refusals stay `AUTH-101` with no details. On MCP the same keys arrive as top-level fields of the tool-error envelope (`plan_required`, `feature_not_available`, `quota_exceeded`) beside the fields each envelope already had; `quota_exceeded` omits `null` values. A contract test fails when a refusal site is added without being classified. Contract: [Gate refusals](docs/api-surface-1.0/error-responses.md#gate-refusals-1644); a shorter consumer version is in `docs/api-reference.md`.
- **`feature_enforcement` on `GET /api/v1/workspaces/plans/tiers`** ([#1648](https://github.com/kagura-ai/memory-cloud/issues/1648)): a plan feature listed in the registry is not by itself a runtime gate, and the payload now says which kind each one is. Every tier row carries the same tier-independent map of feature → what happens on a tier *without* it: `enforced` (refused on every deployment), `conditional` (refused only where a deployment setting turns the check on), `degrades` (served with reduced behaviour) or `advertised` (no runtime check; listed for the plan pages only). Today `api_keys`, `oauth` and `secret_store` are `advertised`, `reranking` `degrades` (recall returns unreranked results), `managed_embeddings` `conditional` (refused only with `EMBEDDING_PLATFORM_FALLBACK_REQUIRES_MANAGED_PLAN` on, default off), and the rest `enforced`. The mode is a property of the code, so a `PLAN_<KEY>_FEATURES` override does not move it; a test scanning `backend/src` fails when a declared mode and the real call sites drift apart. Additive — the existing keys and values are byte-for-byte unchanged, no refusal is added and no tier gains or loses a feature. Table: [enforcement modes](docs/deployment.md#what-a-feature-entry-does-at-runtime-enforcement-modes).
- **`/kagura-memory:setup`** ([#1649](https://github.com/kagura-ai/memory-cloud/issues/1649)): a plugin command that configures and verifies the MCP connection and the guardrail hooks — `guide` explains, `setup` acts. It reports what is in effect, asks before every change, and ends with numbers from the server: the context is picked from `list_contexts()`, never a pasted UUID, with an offer to `create_context` (explicit yes required) when the account has none; the guardrail lane is chosen (plugin hooks with `guardrails=off` on the MCP URL, or the server digest); `get_context_info` and `load_guardrails` prove the connection and report the pinned and tool-triggered counts. The Claude Code part detects the MCP entry in effect (`claude mcp list` / `get`, flagging shadowed entries and duplicate plugin installs), recognises OAuth, Bearer-key and CLI-profile (`kagura-mcp`) entries, derives the hooks' `server_url`, applies `?guardrails=off` (warning first that changing an OAuth URL needs a new sign-in) and test-runs the hook. `check` / `--check` / `doctor` is read-only. Account creation and the connection itself are delegated to the Python SDK CLI (`kagura-memory>=0.31.0`: `kagura auth login`, then `kagura setup claude --profile default`); commands that print a token are left for the user to run, and `claude mcp get` output is redacted before it is shown. The client-independent part is fenced off so other clients' adapters can reuse it.

- **`list_contexts` points a fresh account at `create_context`** ([#1658](https://github.com/kagura-ai/memory-cloud/issues/1658)): when the caller can see no context, the success envelope gains an optional `hint` — an owner can call `create_context(name=...)`, an admin must add `is_private=false`, anyone else should ask for a context or access, and a client without `create_context` in its tool list (for example under `?profile=core`) can use the web UI or reconnect without the profile. A caller with no workspace gets a separate hint pointing to the web UI. The hint is absent whenever at least one context is visible, when a `name_contains` filter merely matches nothing, and when the context lookup itself failed (that case is logged and still returns an empty list, as before). Additive; `create_context` stays outside `?profile=core`, and no context is ever created automatically.

### Changed
- **Five refusals answer a different `error` code, two of them a different status** ([#1644](https://github.com/kagura-ai/memory-cloud/issues/1644)): a shared context on a plan without `shared_contexts`, reached through the service rather than the REST route (MCP `create_context`, for one), moves from **422 `VAL-001`** to **403 `FEAT-001`** — what the REST route already answered for the same condition — and its MCP envelope from `validation_error` to `plan_required`; the managed-embeddings refusal (the platform-key embedding fallback denied to a plan without `managed_embeddings`, raised only where `EMBEDDING_PLATFORM_FALLBACK_REQUIRES_MANAGED_PLAN` is on) moves from **500 `CFG-001`** to **403 `FEAT-001`**, since it can never succeed on retry — a genuinely missing credential stays `CFG-001`; `POST /api/v1/workspaces/{workspace_id}/invitations` answers `FEAT-001` instead of `HTTP-403` on a plan without `team_invitations` (status stays 403; the message is now the standard plan-gate sentence) and `QUOTA-001` (`quota_type: "members"`) instead of `HTTP-429` at the member seat cap (status and message unchanged); `POST /api/v1/resource-tokens` answers `QUOTA-001` (`quota_type: "resource_tokens"`) instead of `HTTP-403` at the active-token cap — the status stays **403**, and the message names the tier by its display name instead of the upper-cased key. Every other refusal keeps its code and status; the connector seat cap keeps `403 CONNECTOR-001`. The context-cap message no longer hardcodes "Upgrade to Basic or Pro plan": it names the tier that raises the cap, or drops the sentence when none does.
- **One gate descriptor for plan, flag and quota gates in the web UI** ([#1645](https://github.com/kagura-ai/memory-cloud/issues/1645)): about a dozen pages decided "can this member use X here, and if not, why?" their own way — ranking the tier name, comparing it literally, reading the tier matrix or a `/system/info` flag — so an operator who moved a feature to another tier was contradicted by the UI. Every gate decision now goes through one resolver, checked in a fixed order: deployment switch, pending inputs, role, plan, quota. Visible effects: the shared-context option in both create-context dialogs, the reranker controls and the invite button's plan suffix stay inert until the plan resolves, so the members page no longer shows "(Pro Plan)" to entitled workspaces while loading; role outranks plan on the invite control and the sleep-reports page (a member or viewer gets role copy, not an upsell); the sleep-reports list waits for the plan answer; tier labels in the resources, connectors, resource-token, context-settings and sleep-reports notices come from the tier matrix — identical on the default matrix, the tier that actually has the feature under an override, and omitted when no tier has it; gates follow the matrix rather than the tier name, so a plan name the matrix does not know fails closed. If the tier matrix cannot load, gates stay pending and never upsell; if `/system/info` fails, default-off flags read as off.
- **The web UI reads `details.gate` instead of status codes and English prose** ([#1644](https://github.com/kagura-ai/memory-cloud/issues/1644)): the gate is derived once, where an API error is built — from `details.gate`, or on an older server from the semantic error code alone; a bare 403, 429 or 402 is never read as a gate. The contexts page no longer parses the server's English message with regexes (the cap message uses the workspace's current tier label and the limit from the refusal); the workspace-create form, the context-settings shared/public toast (tier taken from the refusal, not a hardcoded map) and the invite and connector create errors (plan and seat-cap refusals now localized) read it too. On the members page the admin-only reads swallow only a role refusal; any other 403 is logged instead of silently hidden.
- **The guardrail hook names the endpoint on a 404 / 405** ([#1649](https://github.com/kagura-ai/memory-cloud/issues/1649)): when the guardrail fetch answers `http 404` or `http 405` — a `server_url` pointing at the site root — the notice now reads `server_url must be the MCP endpoint, e.g. https://<host>/mcp or https://<host>/mcp/w/<workspace-id>, not the site root` instead of `server unreachable`. The plugin's `server_url` and `context_id` setting descriptions say the same; `docs/troubleshooting.md` gains "Claude Code — kagura-memory hooks never run", and the migration note in `docs/mcp-clients.md` covers the `kagura-mcp` entry.
- **One gate notice and one `gate.*` namespace in the web UI** ([#1646](https://github.com/kagura-ai/memory-cloud/issues/1646), [#1641](https://github.com/kagura-ai/memory-cloud/issues/1641)): nine hand-rolled treatments of "not on your plan" and five of "limit reached" become one `FeatureGateNotice` component (inline, page and control variants, plus a toast helper) over the gate descriptor, with one `gate.*` message namespace keyed by reason — plan, quota, deployment, role, allowlist. No gate copy names a hardcoded tier any more: the tier comes from the tier matrix, and when no tier includes a feature the notice says so and offers no upgrade. An upgrade button appears only for plan and quota refusals the viewer can act on; deployment, role and rollout-allowlist notices never carry one. Per page: the resources, connectors and resource-token notices are titled, dark-mode-readable alerts; the contexts page shows one notice at the context cap (the yellow emoji banner and the cap dialog are gone; the create items are disabled and point at it) and one shared privacy choice in both create dialogs; the members page shows the invite reason beside the button and one notice at the seat cap (the emoji seat block is now a single usage line); sleep reports, the cost page and the Plan page with `ENABLE_PLAN_PAGE` off use the page notice (the cost page shows one deployment notice where it had two wordings); the Analyses tab and the new-analysis modal no longer mention the allowlist; the dashboard usage header shows the configured plan label instead of an upper-cased key; the Coming-soon page and sidebar labels are translated. Superseded gate copy is deleted from both locales (77 keys each), and a CI check now fails on an English/Japanese key mismatch and on any newly unreferenced message key (existing unreferenced keys are listed in a baseline that may only shrink).

### Fixed
- **A plan refusal in the Analyses tab read as "not yet enabled"** ([#1644](https://github.com/kagura-ai/memory-cloud/issues/1644)): the tab could not tell a plan refusal from the rollout allowlist, so a workspace whose plan lacked analyses was told the feature was not yet enabled and was never shown an upgrade path, and a non-owner was told the same. The three allowlist refusals (REST start, REST read, the MCP gate) now carry `gate: "allowlist"`, and the tab splits three ways: a plan refusal names the tier that has analyses, a role refusal gets owner-only copy, and an allowlist refusal — like a 403 with no gate, or an older server's `FEAT-001` that names no tier — keeps the plan-neutral, CTA-free "not yet enabled" copy. The new-analysis modal shows the daily analysis quota only for `quota_type: "memory_analysis"`, so the REST daily API cap is no longer presented as the analysis quota.
- **Upgrade prompts led to a page that is not there** ([#1643](https://github.com/kagura-ai/memory-cloud/issues/1643)): every plan and quota upsell linked to `/workspace/settings/plan`, which exists only with `ENABLE_PLAN_PAGE=true` (default off for self-hosted) and loads only for the workspace owner, but only the sidebar checked. Upgrade buttons and links now render only when `features.plan_page` is on and the viewer is the owner, never while `/system/info` is pending, and not after it fails. The explanation stays — titles, descriptions, badges, usage numbers; only the action is withheld, except in the contexts quota dialog, whose "See the Plan page" block goes with the button (the remaining button reads Close). Covers the contexts quota banner and dialog, the context-privacy "Upgrade to Pro" buttons, the members seat-limit links (no longer a `<button>` nested in a link), the sleep-reports empty state, the connectors and resources plan gates, the reranker sentence, the resource-token link and the dashboard quota warning. No API change; a backend that omits `features.plan_page` reads as off.
- **The plan comparison table listed features the deployment has switched off** ([#1654](https://github.com/kagura-ai/memory-cloud/issues/1654)): a row is now hidden for every tier once `/system/info` says the deployment's flag is off — the reranker row on `features.reranking: false` (`ENABLE_RERANKING` off, or no reranker configured), the managed-LLM analysis row unless `features.managed_llm` is true (no `MANAGED_LLM_PROVIDER`, which includes default self-hosted deployments, or `/system/info` failed). While `/system/info` is pending the table shows its loading state, so a row is never shown and then withdrawn; its load-error banner is now translated.
- **The Japanese reranker plan-gate sentence ended in an English link** ([#1642](https://github.com/kagura-ai/memory-cloud/issues/1642)): the context search settings spliced the plan link into the translated sentence by splitting it on the English literal "Basic plan", which the Japanese string does not contain, so a Japanese reader saw the whole sentence followed by an untranslated "Basic plan" link. The sentence and its link are now translated as one message in both locales.
- **The dashboard quota warning was English-only** ([#1647](https://github.com/kagura-ai/memory-cloud/issues/1647)): `QuotaWarning` hardcoded its titles, body and button and lowercased the resource name into an English sentence. All six strings are translated and the resource name is passed in already translated, so each locale writes its own sentence. Thresholds and layout are unchanged; the unused `plan.*` message namespace is removed.

### Notes
- No migration, no new environment variables, no operator action.
- **API clients:** `details.gate` and the fields beside it (REST `details` and MCP error envelopes) and `feature_enforcement` on `GET /api/v1/workspaces/plans/tiers` are additive. The five refusals whose `error` code — and for two, status — moved are listed under **Changed**. From this release on, a `QUOTA-001` without `gate` is a limit no tier lifts; offer no upgrade for it, for `gate: "allowlist"` or `"deployment"`, or when `required_plan` is `null`.
- **Deploy order:** deploy the API before (or with) the web client. A web client from this release served against an older API shows the server's own English message for refusals the older API sends without `details.gate` — among them the context cap, the workspace cap and the context-settings shared/public toast; they become localized notices once the API is on this release.
- **Plugins:** the Claude Code and Codex plugin manifests are bumped in lockstep. Update the plugin to get `/kagura-memory:setup`, the endpoint message on a 404 / 405 and the reworded `server_url` / `context_id` descriptions.

## [v0.74.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.74.0) — 2026-09-22

Tool guardrails: a memory marked with `details.tool_trigger` reaches the model at the matching tool call — as the reason for a denied call, or as context next to its result — through Claude Code plugin hooks, Codex plugin hooks, and server-side digests for MCP clients that run no hooks. Also on the MCP surface: a failed `tools/call` now carries `isError`.

### Added
- **Tool guardrails — the server contract** ([#1619](https://github.com/kagura-ai/memory-cloud/issues/1619)): a memory becomes a guardrail by carrying `details.tool_trigger = {tool, on?, match?, action?}` — `tool` is a regex full-matched against the tool name the client reports (`Bash|PowerShell`, `mcp__.*__remember`, `Edit|Write`), `on` is `pre` (default) or `result`, `match` an unanchored regex over the call's input or result text, `action` is `inform` (default) or `block` (`block` needs `on: "pre"` and a `match` that names a literal). Both patterns are validated on every write against a safe-regex grammar — the subset that behaves the same in Python `re` and JavaScript `RegExp`, with no backreferences, lookaround, named or atomic groups, nested quantifiers or ambiguous unbounded runs; a stable `regex_*` / `tool_trigger_*` code names the violated rule. The server compiles a pattern once to validate it and never runs it: **matching happens only in the client.** The new deterministic read — MCP `load_guardrails(context_id, cap?)` and REST `POST /api/v1/memory/guardrails` — returns the trusted-tier pinned set and the tool-triggered set as two independently capped lanes, with a `format: 1` payload and a per-credential `version` hash that every client caches; connector-ingested rows and contexts whose `trust_tier` is not `trusted` are never served, ingest strips `tool_trigger`, and Sleep never merges, archives or re-scores a guardrail. Adding, changing or removing `tool_trigger` — and any edit or delete of a memory that already carries one — needs **context editor or above** and a user credential (`permission_denied` with `required_role: "editor"` on MCP, `403` on REST; an agent-bound key gets `tool_trigger_requires_user_credential`); `forget` keeps its silent contract and skips a guardrail the caller may not delete. Contract: [Tool guardrails](docs/mcp-tools.md#tool-guardrails).
- **Claude Code plugin hooks** ([#1619](https://github.com/kagura-ai/memory-cloud/issues/1619)): the `kagura-memory` plugin declares hooks (`claude-hooks/hooks.json`) that run one stdlib Python script behind a POSIX `sh` guard. `SessionStart` calls `load_guardrails` once over the configured MCP URL, caches the set under the plugin's data directory (`guardrails/<context_id>.json`, mode `0600`), adds one line to Claude's context (how many guardrails are active) and shows the user a one-line notice naming new, changed or removed guardrails (`(by another member)` for someone else's); with the server down a cache up to 24 h old is used. `PreToolUse` / `PostToolUse` / `PostToolUseFailure` match locally against the cache — no network, tool inputs never leave the machine: `inform` arrives as `additionalContext` next to the tool result, `block` denies the call once with the memory summary as the reason (a deny costs one model turn), each guardrail once per session and agent; `/clear` and `/compact` reset the main thread. A `remember` / `update_memory` / `forget` call refreshes the set in the background. Credentials come only from the plugin's `userConfig` (`server_url`, `api_key`, `context_id`, `max_action` = `block` | `inform`); `.mcp.json`, `~/.claude.json`, project settings and `KAGURA_*` variables are never read, and `python3` is resolved to a path outside the project. Needs `python3` 3.9+ on `PATH`; Windows is unsupported for these hooks. Put `guardrails=off` in the `.mcp.json` URL's query (`?guardrails=off`, or `&guardrails=off` when the URL already has one, such as `?profile=core`) so the server does not also send a digest. Setup: "Claude Code Plugin › Tool guardrails (hooks)" in [MCP Client Setup](docs/mcp-clients.md).
- **Codex plugin hooks** ([#1620](https://github.com/kagura-ai/memory-cloud/issues/1620)): the Codex plugin (`plugins/kagura-memory`) gains `hooks/hooks.json` and a Codex adapter for the same script (`--client codex`), so Codex CLI and the ChatGPT desktop app's local Codex runtime receive guardrails at the matching tool call from the same cache, matcher and renderer: `SessionStart`, `PreToolUse` / `PostToolUse` on every tool, and one async refresh on `remember` / `update_memory` / `forget`. `Bash` is matched on its command, `apply_patch` as `Edit` / `Write` with one subject per file header, any other tool on its compact JSON input; the deny reason never repeats the command (Codex appends it). Settings (`context_id`, `max_action`, `mcp_server`) live in `${PLUGIN_DATA}/config.json`; credentials are read from the user's own `[mcp_servers.<name>]` table in `$CODEX_HOME/config.toml` — `url` plus exactly one Authorization source (`bearer_token_env_var`, `env_http_headers.Authorization` or `http_headers.Authorization`); anything else is one `SessionStart` notice naming the field and zero requests. Only `SessionStart` and the refresh ever read `config.toml` or open a socket; tool events read the cache alone. Reading `config.toml` needs Python 3.11+ (`tomllib`); on an older interpreter `SessionStart` prints one notice and tool events keep running from the cache. Codex lists the four hook entries as untrusted until you accept them once in `/hooks`; the file is byte-pinned by a test so that every change to it — which sends every user back through `/hooks` — is deliberate.
- **Guardrails for MCP clients without tool hooks** ([#1621](https://github.com/kagura-ai/memory-cloud/issues/1621)): ChatGPT web, Claude Desktop / Claude Chat, Codex cloud tasks and most other MCP clients run no hooks, so a guardrail reached their model only if the model happened to recall it. The server now renders the same trusted-only, binding-filtered tool-triggered set (`load_guardrails.tool_triggered`) on the three model-visible lanes those clients have. **Server `instructions`** — `initialize` and `server/discover` return the base text plus a digest of one context's guardrails (up to 5 entries, one line each, 1,200 characters at most; the first 512 always hold the header and the whole first entry) when the endpoint URL selects it, `…/mcp/w/{workspace_id}?guardrails=<context_id>` (same query as `?profile=`), or, with the parameter absent, when the credential is an agent-bound key with a default or sole binding. **`get_context_info(context_id).guardrails`** — on by default: `{items: [{memory_id, summary, importance, authored_by_caller, source_type}], total_available, truncated, tool_triggered_version}`, up to 10 entries; the session-start skills fold it in after `load_pinned`. **Export block** — `GET /api/v1/memory/guardrails/digest?context_id=<uuid>` returns an `AGENTS.md` block (`text/markdown`, up to 20 entries) for a Codex cloud setup script — the documented recipe writes it between two marker lines and keeps it out of the task's diff — and `&target=instructions` previews exactly what the `instructions` lane serves the calling credential. One switch: `?guardrails=off` turns both server lanes off (the setting for a client whose plugin hooks already deliver guardrails); a value that is neither `off` nor a context id is ignored and never folded into `off`. The `instructions` lane is fail-open: a denied, unknown or external-tier context, an empty set, a database error or the digest budget expiring all serve the base text, and the handshake never fails; `get_context_info.guardrails` is `null` when the read fails or no context resolves, and the export endpoint answers a denied or unknown context with the uniform `404`. Summaries only — never `content`, `details`, tags or patterns. Rules: [Server instructions](docs/mcp-tools.md#server-instructions); per client: [MCP Client Setup](docs/mcp-clients.md).

### Changed
- **The MCP server's `instructions` text is reworded and is now returned by `initialize` too** ([#1621](https://github.com/kagura-ai/memory-cloud/issues/1621)): the base text points a client to `get_context_info(context_id)` for a context's rules and guardrails before `remember` / `recall` / `explore`; before this release only `server/discover` carried `instructions`. With no `guardrails=` selection and no agent-bound key, `server/discover` keeps its public one-hour cache hints and is byte-identical for every caller; whenever a selection was attempted (any `guardrails=` value, including `off`, or an agent-bound key) the result is `cacheScope: "private"` for 5 minutes, whatever the outcome — a denied caller and an allowed one never share a cache entry. `tools/list` is untouched.
- **The dev `docker-compose.yml` publishes the data stores on loopback only** ([#1626](https://github.com/kagura-ai/memory-cloud/issues/1626)): PostgreSQL 5432, Qdrant 6333 / 6334, Redis 6379 and MinIO 9000 / 9001 are published as `${COMPOSE_BIND_HOST:-127.0.0.1}:PORT:PORT` instead of on every interface — in that file Redis and Qdrant run without authentication, PostgreSQL uses the `.env.example` password, and Docker's `nat` rules run ahead of a host firewall. The API (8080) and web (3000) entries and the production compose files under `terraform/single-server/` are unchanged; a new bats guard in the CI `shell` job rejects a tracked compose file that publishes a data-store port on an implicit or all-interface bind (a loopback default or a required-address variable such as the split-host overlay's `${DATA_BIND_ADDR:?}` passes). `docs/getting-started.md` gains "Reaching the data stores" (`docker compose exec`, `ssh -L`, the `COMPOSE_BIND_HOST` override, an IPv6 and a WSL2 note).

### Fixed
- **Failed tool calls are flagged with `isError: true`** ([#1622](https://github.com/kagura-ai/memory-cloud/issues/1622)): both MCP transports serialised a tool error as a plain successful `CallToolResult`, so a client could not tell a failed `tools/call` from a successful one without parsing the private `{"status": "error", …}` envelope, and hook runners keyed on `isError` (Claude Code's `PostToolUseFailure`) never fired. Every error envelope — `unknown_tool`, `invalid_context_id_format`, `context_id_required`, a permission error, a handler exception — now carries `isError: true`; `content` is byte-identical to before, success results carry no `isError` key, and JSON-RPC protocol errors are unchanged. Described in `docs/api-surface-1.0/error-responses.md`.
- **The tag near-duplicate hint fired on compound tags** ([#1617](https://github.com/kagura-ai/memory-cloud/issues/1617)): `session-cookie` next to `session`, `cache-layer-redis` next to `cache-layer`, `session-2026-09-11` next to `session` — a topic and a sub-topic, not two spellings of one tag — and, because the generic tag usually has the highest count, the false hint masked the real mechanical variant (`session_cookie`). The write lint now skips a pair where one tag's segments (split on whitespace, `_`, `-`, `/`, `:`, `#`; not `.`, so `node` / `node.js` still hints) are a whole-segment prefix of the other's, and picks an equal-fold match (`session_cookie`) ahead of the most-used one. Numbered tags keep the v0.73.1 rule (`session-2026-09` / `session-2026-09-21` stay related). The read path is unchanged: a narrower or broader stored tag stays a valid `suggest_tags` answer for an empty tag-filtered `recall`.
- **The External Keys page had no sidebar entry with BYOK off** ([#1616](https://github.com/kagura-ai/memory-cloud/issues/1616)): with `ENABLE_BYOK=false` the page is still the workspace owner's console for keys stored before the flag was turned off (list / toggle / delete answer; only create / update return `404`), and in the default `RESOLVE_STORED_BYOK_KEYS=true` posture those keys are still in use — but the sidebar dropped the entry on the flag alone, so the page was reachable only by typing its URL. The entry now shows for the workspace **owner** when `features.byok` is on **or** the workspace already stores at least one key (one owner-only list probe per workspace per page load, only on a BYOK-off deployment), and stays hidden while either answer is pending. No backend or API change.
- **The Codex CLI setup snippets failed to load on current Codex** ([#1624](https://github.com/kagura-ai/memory-cloud/issues/1624)): the manual `~/.codex/config.toml` entry set an inline `bearer_token`, which Codex rejects for a streamable-HTTP server (`bearer_token is not supported for streamable_http`) and which makes the whole `config.toml` fail to load, plus a `type = "http"` key Codex does not know; the recommended `codex plugin install` is not a subcommand; and the Web UI's Codex tab rendered the same two defects with the API key embedded in the file. Docs, the Web UI Codex tab and the plugin `SKILL.md` now agree on `codex mcp add kagura-memory --url "<url>" --bearer-token-env-var KAGURA_API_KEY`, with a two-key `[mcp_servers.kagura-memory]` (`url`, `bearer_token_env_var`) as the manual form — the key stays out of the file — checked against Codex `rust-v0.155.1`. `docs/troubleshooting.md` gains an entry keyed to the error above, and a guard test parses every published TOML snippet against Codex's key allowlist.

### Notes
- **Migration:** `alembic upgrade head` — `e84_1619_tool_guardrails`: adds the partial index `idx_memories_tool_trigger` on `memories` (built `CONCURRENTLY`, no table lock; re-runnable after a mid-build failure) and admits `load_guardrails` to the `memory_access_events` operation CHECK (swapped `NOT VALID`, then `VALIDATE`, so audit writes continue). Reversible: downgrade drops the index and restores the previous CHECK while keeping the audit rows.
- **New env knobs** (all optional): `GUARDRAIL_LOAD_CAP` (default `50` — cap on the tool-triggered lane of `load_guardrails`, kept separate from the pinned lane's cap so a large pinned set cannot crowd the guardrails out), `MCP_GUARDRAIL_DIGEST_ENABLED` (default `true` — the `instructions` lane only; `get_context_info.guardrails` and the export route stay served) and `MCP_GUARDRAIL_DIGEST_TIMEOUT_MS` (default `500` — the budget for the one database read on `initialize` / `server/discover`; on expiry the base text is served).
- **MCP surface:** 63 → 64 tools (`load_guardrails`, listed after `load_pinned`; exempt from rate limiting; not part of `?profile=core`). The full tool list measures 83,486 characters, within the 84,000 budget the size-guard tests pin. New REST routes `POST /api/v1/memory/guardrails` and `GET /api/v1/memory/guardrails/digest`. New validation codes `tool_trigger_*` / `regex_*` (the stable `<code>` token inside `invalid details.tool_trigger: <code>: <sentence>`), `permission_denied` with `required_role: "editor"` on guardrail writes, and the audit operation `load_guardrails` in `memory_access_events`.
- **Plugins:** the Claude Code and Codex plugin manifests are bumped in lockstep. The Claude Code plugin now declares `hooks` and a `userConfig` — Claude Code asks for `server_url`, `api_key` (stored as a sensitive value), `context_id` and `max_action` (default `block`) when the plugin is enabled (`/plugin` → kagura-memory → configure), or takes them from `claude plugin install … --config key=value`; with nothing configured the hooks are silent. The Codex plugin declares `hooks`, which Codex lists as untrusted until accepted in `/hooks`. A client whose plugin hooks deliver guardrails should carry `guardrails=off` in its MCP URL's query (`?guardrails=off`, or `&guardrails=off` after an existing query) so the server does not deliver the same guardrails a second time.
- **Dev environment:** after this release the dev data stores answer on `127.0.0.1` only. A setup that reached PostgreSQL / Qdrant / Redis / MinIO from another machine sets `COMPOSE_BIND_HOST` (shell or project `.env`) or uses an SSH port-forward to the host's loopback; anyone using `docker-compose.yml` as a deployment file with remote `psql` / Qdrant access has the same two options or moves to `terraform/single-server`. Production compose is unchanged.
- **Developer tooling:** the Bash `PreToolUse` reminder hook in `.claude/settings.json` now reaches the model — it returns `hookSpecificOutput.additionalContext` on stdout (stderr at exit 0 is debug-log only), fires on `gh pr create` alone, and reads its stdin without `echo`, which under `dash` broke the JSON for any multi-line command ([#1623](https://github.com/kagura-ai/memory-cloud/issues/1623)); the `/release` skill is rewritten for the PR-based flow used since v0.70.0 (release branch → PR → squash → annotated tag → GitHub Release → milestone close), with a lockstep test over the seven version files and the CHANGELOG heading ([#1625](https://github.com/kagura-ai/memory-cloud/issues/1625)); four places documented the version endpoint as `GET /api/v1/info` — it is `GET /api/v1/system/info` ([#1618](https://github.com/kagura-ai/memory-cloud/pull/1618)).

## [v0.73.1](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.73.1) — 2026-09-21

Follow-ups to the token-economy release, plus a credential that could not be withdrawn.

### Changed
- **The core tool profile is offered wherever the endpoint URL is shown** ([#1609](https://github.com/kagura-ai/memory-cloud/issues/1609)): the web UI's MCP config block has a switch, "Core tools only (smaller tool list)" (default off) — when on, every rendered and copied URL carries `?profile=core`. README, `docs/getting-started.md` and the per-client sections of `docs/mcp-clients.md` show both URLs with one wording; `docs/troubleshooting.md` gains "A tool I expect is missing from my client". The server default is unchanged (all tools).
- **`GET /api/v1/external-keys` items carry `is_protected`** ([#1613](https://github.com/kagura-ai/memory-cloud/issues/1613)) (also on the create / update / toggle responses; additive). The External Keys page shows the Required badge and locks delete / disable only when it is true.

### Fixed
- **`OPENAI_API_KEY` could never be deleted or disabled** ([#1613](https://github.com/kagura-ai/memory-cloud/issues/1613)), even on a deployment whose embeddings do not use OpenAI or that no longer reads stored keys, so an owner could not withdraw their own credential. It is now protected only while `ENABLE_BYOK` and `RESOLVE_STORED_BYOK_KEYS` are on **and** OpenAI embeddings are in use — `EMBEDDING_PROVIDER=openai`, or a live context of the workspace routes to an OpenAI embedding model. Delete and disable share the rule (the disable guard also follows the row's provider, since the embedding service picks the stored key by provider). Refusals keep status `400` and their shape; the message states the actual reason. With `ENABLE_BYOK=false` the owner's list / toggle / delete paths were already reachable — stored keys can now be withdrawn through them.
- **The tag near-duplicate hint fired on numbered tags** ([#1608](https://github.com/kagura-ai/memory-cloud/issues/1608)): `issue:#1599` "resembled" `issue:#179`, `v0.73.0` resembled `v0.69.0`, `session-2026-09-21` resembled `session-2026-05-20` — false hints on almost every write in a workspace that tags by issue, version or date, and the same noise in `tag_suggestions` on an empty tag-filtered `recall`. Two tags of the same shape whose numbers differ in value are no longer related; the same number written two ways (`sprint-07` / `sprint-7`, full-width digits) still is, and so are abbreviations and typos (`dev-env` / `dev-environment`, `kuberentes` / `kubernetes`).

### Notes
- No migration, no new environment variables, no operator action.

## [v0.73.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.73.0) — 2026-09-21

Token economy of the MCP surface. Everything a tool returns — and the tool list itself — is paid for in the calling model's context window; this release removes the avoidable part. Several response shapes change: read **Changed** before upgrading a client that parses tool results.

### Added
- **Tool profiles** ([#1601](https://github.com/kagura-ai/memory-cloud/issues/1601)): the endpoint URL in a client's local MCP config can select which tools `tools/list` returns — `…/mcp/w/{workspace_id}?profile=core` (the 12 memory / context tools) or `?tools=remember,recall,reference` (explicit allowlist, wins over `profile`). No parameter, or `profile=full`, is byte-for-byte the previous list, on both the session-based and the stateless (2026-07-28) transports. An unknown profile, or an allowlist that matches no known tool, is a JSON-RPC `-32602` naming the valid values; unknown names inside an allowlist are ignored. The profile is a **view, not an authorization boundary**: `tools/call` never reads it and keeps its role checks. See [Tool Profiles](docs/mcp-tools.md#tool-profiles) and the per-client examples in `docs/mcp-clients.md`.
- **`list_contexts(name_contains=…)`** ([#1600](https://github.com/kagura-ai/memory-cloud/issues/1600)): case-insensitive substring filter, applied after the permission-scoped list; plus `include_summary` (summary truncated to 300 characters, `summary_truncated` flags a cut) and `include_details` (the previous per-item shape). The envelope gains `total` (contexts in the response).
- **`recall_upcoming(include_details=true)`** ([#1599](https://github.com/kagura-ai/memory-cloud/issues/1599)) returns the full `details` per item, as before this release.

### Changed
- **Tool results are compact UTF-8 JSON** ([#1599](https://github.com/kagura-ai/memory-cloud/issues/1599)): non-ASCII text (Japanese, for instance) arrives as-is instead of as 6-character `\uXXXX` escapes, and separators carry no spaces. Any JSON parser reads both forms identically; only a client matching raw substrings such as `"status": "success"` would notice. The JSON-RPC transport body is unchanged.
- **`recall`** ([#1599](https://github.com/kagura-ai/memory-cloud/issues/1599)): `related_tags[]` entries are `{tag, count}` — `sample_summary` repeated, in full, a summary already present in `results[]`. Result items **omit** `context_summary`, `superseded_by`, `contradicts` and `supersede_candidate` when empty (treat an absent key as "none"), and `score` is rounded to 4 decimals. MCP surface only: REST `/memory/recall` and the web UI are unchanged.
- **`recall_upcoming`** ([#1599](https://github.com/kagura-ai/memory-cloud/issues/1599)): default items are `{memory_id, summary, type, trigger}` (`trigger` = `details.trigger`). `get_agent_bootstrap`'s upcoming component — MCP tool and REST companion — ships the same lean row.
- **`list_contexts` is a slim name → id directory by default** ([#1600](https://github.com/kagura-ai/memory-cloud/issues/1600)): items are `{id, name, is_private, is_locked, last_used_at}` (+ `memory_count` with `include_stats`). `summary` and `embedding_model` moved behind `include_summary` / `include_details`; for one context's full details call `get_context_info(context_id)`. Plugin skills use the new default.
- **Tool descriptions are trimmed to a token budget** ([#1602](https://github.com/kagura-ai/memory-cloud/issues/1602)): descriptions and parameter descriptions keep what an agent needs to call a tool correctly (purpose, when to use it instead of a neighbour, parameter semantics, response keys, the security rule on writes, the supersede workflow, trusted-tier guidance); tutorials and rationale moved to the new **Usage notes** section of `docs/mcp-tools.md` and to the `guide` / `recall` / `remember` skills. Schema structure is unchanged and pinned by a skeleton snapshot test; budgets are pinned by tests.

### Fixed
- **`list_contexts` could be rejected by the client** ([#1600](https://github.com/kagura-ai/memory-cloud/issues/1600)): with a few dozen contexts the response (every summary, escaped) grew large enough for an MCP client with a tool-result limit to refuse it, which broke the first step of every plugin skill.
- **A non-UTF-8 query string can no longer raise in the legacy session-id lookup** ([#1601](https://github.com/kagura-ai/memory-cloud/issues/1601)).

### Notes
- Measured on the fixtures pinned by the new size-guard tests: a k=5 `recall` 22,779 → 4,553 characters; `list_contexts` with 40 contexts 488,842 → 6,455; `tools/list` for all 63 tools 110,608 → 81,834 characters, and 28,254 with `?profile=core`. The `recall` fixture is mostly Japanese; for ASCII-only content the encoding change is neutral and the saving comes from the removed duplication.
- No migration, no new environment variables, no operator action. The Claude Code / Codex plugin manifests are bumped in lockstep; update the plugin to pick up the skills written for the slim `list_contexts`.

## [v0.72.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.72.0) — 2026-09-20

Closed-beta invites after first real use: the invitee lands in the app instead of back on the login form, and the inviter can tell links apart, resend one, and read an honest count.

### Added
- **A label per invite** ([#1595](https://github.com/kagura-ai/memory-cloud/issues/1595)): `POST /api/v1/beta-invites` takes an optional JSON body `{"label": string | null}` (free text, up to 100 characters after trimming; control characters and text that is not valid Unicode are a `422`). A request with no body behaves exactly as before. The label is stored on the invite, returned on every list item and on the created payload, and rendered on the row. It is inviter-private: it never reaches a log line, an audit row or an error message, and it is deleted with the inviter's account.
- **One-click reissue** ([#1595](https://github.com/kagura-ai/memory-cloud/issues/1595)): `POST /api/v1/beta-invites/{id}/reissue` (session only, no body, `201`, same payload as create) revokes an `active` or `expired` invite and mints a replacement with the same label in one transaction — for a recipient who lost the link, since only the token's hash is stored and the URL cannot be shown again. Errors: `404` (unknown id or someone else's invite), `409 BETA-INVITE-002` (already used), new `409 BETA-INVITE-003` (`details.reason = "already_revoked"` — a double-click cannot mint a second link), `409 BETA-INVITE-001` when an expired invite is reissued at the cap (the revoke is rolled back). The audit log records one `beta_invite.revoked` and one `beta_invite.created` row that cross-reference the two invites by id. The dialog shows a Reissue button on active and expired rows and presents the new URL in the same one-time panel.
- **The inviter sees who an invite admitted** ([#1595](https://github.com/kagura-ai/memory-cloud/issues/1595)): a `redeemed` item in `GET /api/v1/beta-invites/me` carries `redeemed_email`, the admitted account's current e-mail. It is read from the live account only — never from the allowlist snapshot — so it becomes `null` when that account is erased. This deliberately replaces the v0.70.0 "status only" contract: the inviter is the person who vouched for the invitee.

### Changed
- **The invite header is a breakdown, not a bare "Used N"** ([#1595](https://github.com/kagura-ai/memory-cloud/issues/1595)): `GET /api/v1/beta-invites/me` gains `active` and `redeemed` (`active + redeemed == used` by construction; `used` / `remaining` / `quota` are unchanged), and the dialog reads `Active 2 · Used 1 · Limit 4` / `有効 2 · 使用済み 1 · 上限 4` (`No limit` / `上限なし` for a system admin). The old figure counted slots in use, so one redemption plus two unused links read as "3".

### Fixed
- **An invitee no longer lands on the login form after signing up** ([#1594](https://github.com/kagura-ai/memory-cloud/issues/1594)): `/join/[token]` now returns to `/workspace/dashboard` after the OAuth round trip. Before, it returned to `/`, which redirects to `/login`, which ignored the session that had just been created — and a second sign-in from there invalidated it.
- **`/login` forwards a visitor who is already signed in** ([#1594](https://github.com/kagura-ai/memory-cloud/issues/1594)): to the sanitized `return_to`, default `/workspace/dashboard`, without rendering the form. `?error=…` and `?cancelled=1` still render their banners (an `email_in_use` error can arrive while another account's session is live). The page shows a labelled spinner until the session check settles and renders the form after 3 seconds regardless, so a slow or failing `/auth/me` cannot lock anyone out.
- **`return_to` hardening** ([#1594](https://github.com/kagura-ai/memory-cloud/issues/1594)): the client-side `safeReturnTo` guard now rejects a backslash or any C0 control character (a URL parser reads `\` as `/` and drops TAB / LF / CR, so `/\host` resolves cross-origin), matching and exceeding the backend's redirect validator; the new `/login` forward additionally resolves its target same-origin before navigating. Found in pre-merge review of the forward, before it shipped.

### Notes
- **Migration:** `alembic upgrade head` — `e83_1595_beta_invite_label` (adds the nullable `beta_invites.label` column; no backfill). Downgrade drops the column and keeps the invites.
- No new environment variables. `ENABLE_BETA_INVITES=false` 404s the reissue route like the others, before authentication.
- API clients: all `beta-invites` payload changes are additive.

## [v0.71.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.71.0) — 2026-09-20

Operational hygiene for the single-server template: bounded container logs, and proxy logs that never hold an invite token.

### Fixed
- **Container logs are bounded** ([#1591](https://github.com/kagura-ai/memory-cloud/issues/1591)): every service in `terraform/single-server/docker-compose.app.yml`, `docker-compose.prod.yml`, `docker-compose.data.yml` and `docker-compose.ollama.yml` declares `logging: json-file` with `max-size: 50m` / `max-file: 3` through a per-file `x-logging` anchor. A host that was not provisioned with a Docker `daemon.json` no longer lets the proxy's JSON access log grow until the disk fills. The options take effect when a container is (re)created.
- **The Caddy template scrubs invite tokens from its logs** ([#1591](https://github.com/kagura-ai/memory-cloud/issues/1591)): `Caddyfile.tpl` logs through a `filter` encoder that replaces the token slot of `/join/<token>`, `/beta-invites/<token>/preview` and `invite=<token>` with `REDACTED` in `request>uri` and in the `Location` / `Refresh` response headers (a trailing-slash redirect echoes the path), and drops the `Referer`, `Cookie`, `Next-Url` and `Next-Router-State-Tree` request headers. The same scrub is applied to Caddy's **default logger**: when an upstream is down, Caddy writes a second `http.log.error` line about the request to stderr with the full URI and headers, so filtering the access log alone left a copy of the token in the same container log. Ordinary URIs are logged unchanged.

### Notes
- **Applying to a running stack:** re-render the Caddyfile and reload Caddy for the log format (no recreate needed). For rotation, run `docker compose … up -d --force-recreate caddy web` once — the API colours are recreated by every `deploy.sh` run; recreating a container also discards its old oversized log file; `postgres` / `qdrant` / `redis` restart when recreated, so do that in a maintenance window. A host-level `daemon.json` with the same limits remains a good default.
- **Caddy 2.6.2+** is required for the response-header redaction (an older 2.x image silently skips it; the URI rewrite and the header deletes still work) — `docker compose pull caddy`.
- No application code, API, migration or environment-variable change. With this template the proxy-log precondition for `ENABLE_BETA_INVITES` (see v0.70.0) is met out of the box; if you run a different proxy, replicate the scrub for both its access log and its error log. See Deployment → *Container log rotation* and *Closed-beta invite links*.
- Two new bats suites (static pins, and a live suite against `caddy:2-alpine` including the 502 path) run in the CI `shell` job.

## [v0.70.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.70.0) — 2026-09-19

Closed-beta invite links, an admin environment console that tells the truth, and a sharing gate that no longer blocks unrelated context edits.

### Added
- **Closed-beta invite links** ([#1581](https://github.com/kagura-ai/memory-cloud/issues/1581), [#1582](https://github.com/kagura-ai/memory-cloud/issues/1582)): with `ENABLE_BETA_INVITES=true` a signed-in user mints one-time `/join/{token}` links (7-day TTL) that let one new person through a closed signup gate. Each user holds up to `BETA_INVITE_QUOTA_PER_USER` invites (default 4, counting active + redeemed; expired and revoked invites free their slot; system admins are uncapped). Only the SHA-256 of the token is stored and the URL is returned once. Redemption happens at the OAuth callback, only where the gate would otherwise block, and records the invitee in `signup_allowlist` as `source='beta_invite'` keyed on the immutable IdP identity (never the e-mail, [#655](https://github.com/kagura-ai/memory-cloud/issues/655)) and attributed to the inviter. API: `GET /api/v1/beta-invites/me`, `POST /api/v1/beta-invites`, `DELETE /api/v1/beta-invites/{id}`, public `GET /api/v1/beta-invites/{token}/preview` (per-IP rate limited), `invite=` on `GET /api/v1/auth/{provider}/login`; `features.beta_invites` on `/system/info`. The inviter sees status only (`active` / `redeemed` / `expired` / `revoked`), never the invitee.
- **Invite UI** ([#1582](https://github.com/kagura-ai/memory-cloud/issues/1582)): an "Invite a friend" card above the account menu (dismissable, hidden at the cap), a permanent account-menu entry with the `n/4` counter, a dialog that shows the link once with Copy and lists/revokes invites, and the public `/join/[token]` landing page. `/signup-blocked` points invitees back to their link. Nothing renders and nothing is requested while the flag is off or unknown.
- **`/join/*` sends no `Referer` and is not indexed** ([#1588](https://github.com/kagura-ai/memory-cloud/issues/1588)): the landing route is served with `Referrer-Policy: no-referrer` and `X-Robots-Tag: noindex, nofollow`, repeated as `<meta>` tags for deployments whose proxy overwrites response headers, so the invite token is not copied into a header that proxies log.
- **Invite tokens are scrubbed from logs and `usage_stats`** ([#1581](https://github.com/kagura-ai/memory-cloud/issues/1581)): structlog events, the stdlib / uvicorn access log and the persisted request path show the literal `{token}` for the three invite URL shapes.
- **Hosted-mode settings in the admin environment console** ([#1580](https://github.com/kagura-ai/memory-cloud/issues/1580)): system admins see a read-only `hosted` category (`ENABLE_BYOK`, `RESOLVE_STORED_BYOK_KEYS`, `ENABLE_COST_DISPLAY`, `MANAGED_LLM_*`, `DEFAULT_RERANKER_*` / `DEFAULT_USE_RERANK`, `RERANK_BASE_URL` with credentials masked, `RERANK_MODEL`, `ENABLE_PLAN_PAGE`). Non-admin callers of `GET /config` see the same keys as before.

### Changed
- **`/admin/environment` is read-only and shows effective values** ([#1580](https://github.com/kagura-ai/memory-cloud/issues/1580)): `GET /api/v1/config` renders what `get_settings()` actually uses (items gain `read_only: true`); `PUT /api/v1/config/{key}` and `POST /api/v1/config/batch` answer **409 `CFG-002`** instead of 200. Values saved from that page were never read by any runtime consumer, so existing `config_overrides` rows were never effective and are now ignored (no migration; the table stays for account-erasure pseudonymisation). Env-only Neural Memory flags (`TRACK_CO_ACTIVATION`, `ENABLE_DECAY`, `ENABLE_TRUST_MODULATION`) now display their effective default instead of "Disabled".
- **The reranker card is hidden when the deployment does not rerank** ([#1580](https://github.com/kagura-ai/memory-cloud/issues/1580)): with `features.reranking=false` the context search settings render no reranker card and make no external-keys request (v0.69.0 showed it disabled with a note). Unknown / still-loading keeps the card.
- **The shared-context gate fires only on the private → shared transition** ([#1583](https://github.com/kagura-ai/memory-cloud/issues/1583)): `PUT /contexts/{id}` with a no-op `is_private: false` on an already-shared context is accepted on a plan without `shared_contexts` (block-new-only, same rule as `is_public`), so unrelated edits such as `sleep_mode` are no longer refused. The Settings tab sends only fields the user touched, disables "Make shared" with an explanation on plans without the feature, and names the control in a FEAT-001 toast.
- **Feature-refusal text uses display names on both sides** ([#1583](https://github.com/kagura-ai/memory-cloud/issues/1583)): "… not available on M plan. Upgrade to L plan …" instead of mixing the raw plan key with the display name. `details.feature` is unchanged.

### Notes
- **Migration:** `alembic upgrade head` — `e82_1581_beta_invites` (new `beta_invites` table; `signup_allowlist.source` CHECK admits `beta_invite`). Downgrade refuses while `beta_invite` allowlist rows exist.
- **New env knobs:** `ENABLE_BETA_INVITES` (default `false` — also the kill switch for minting *and* redemption), `BETA_INVITE_QUOTA_PER_USER` (default `4`, `0` = system admins only). See Deployment → *Closed-beta invite links*.
- **Before enabling invites behind a reverse proxy that writes access logs:** the proxy records the request URI and the `Referer` header, both of which carry the invite token on `/join/{token}`. Scrub those fields or restrict access to the logs first; the application's own logs are already scrubbed, and current browsers send no `Referer` from `/join/*` ([#1588](https://github.com/kagura-ai/memory-cloud/issues/1588)), which leaves the URI as the field that must be filtered.
- **New error codes:** `CFG-002` (409, read-only configuration), `BETA-INVITE-001` (409, quota exceeded) and `BETA-INVITE-002` (409, already redeemed). New audit actions `beta_invite.created` / `.redeemed` / `.revoked`.

## [v0.69.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.69.0) — 2026-09-19

Hosted mode without BYOK: platform-managed reranking, Analysis and Sleep, operator pricing for paid OpenAI-compatible endpoints, and UI gates for money surfaces.

### Added
- **Operator pricing overrides** ([#1570](https://github.com/kagura-ai/memory-cloud/issues/1570)): `LLM_PRICING_OVERRIDES` (JSON array of `{provider, model, unit_type, price_per_unit[, unit_denominator][, context_min_tokens]}`, USD) is validated at boot and appended to `llm_pricing` at API startup when the effective price differs (append-only, cache cleared); `python -m src.cli.sync_llm_pricing [--plan|--apply]` re-syncs without a restart.
- **Reranking as a deployment default** ([#1572](https://github.com/kagura-ai/memory-cloud/issues/1572)): `DEFAULT_RERANKER_PROVIDER` / `DEFAULT_USE_RERANK` / `DEFAULT_RERANKER_MODEL` seed every new or lazily-materialized context search config; with `self_hosted` + `RERANK_BASE_URL` reranking needs no per-workspace key. `GET /api/v1/system/info` exposes `features.reranking` and `search_defaults`; `python -m src.cli.apply_rerank_defaults (--workspace | --all) [--plan|--apply]` converts existing default-valued contexts.
- **Managed LLM lane** ([#1569](https://github.com/kagura-ai/memory-cloud/issues/1569)): `MANAGED_LLM_PROVIDER` (`openai` | `anthropic` | `gemini` | `self_hosted`) + `MANAGED_LLM_MODEL` let Memory Analysis run with zero workspace keys (`paid_by='platform'`, single model, platform credential only) for plans carrying the new `managed_llm` feature (L / XL by default, overridable via `PLAN_<KEY>_FEATURES`); Sleep's judge defaults to the same pair when `SLEEP_LLM_*` is unset. `features.managed_llm` on `/system/info`; `managed_llm` row on the plan matrix.
- **`RESOLVE_STORED_BYOK_KEYS`** ([#1569](https://github.com/kagura-ai/memory-cloud/issues/1569)): opt-in (`false`, requires `ENABLE_BYOK=false`) that makes the LLM, embedding and reranker services ignore stored `external_api_keys` rows, so "BYOK off" can mean off.
- **`ENABLE_COST_DISPLAY`** ([#1571](https://github.com/kagura-ai/memory-cloud/issues/1571), default `true`): when `false` the workspace cost dashboard answers 404, REST analysis cost fields are `null`, MCP analysis payloads omit the cost keys and the web UI shows no `$` to workspace users. `/admin/cost` and `GET /admin/cost-aggregation` are unaffected.
- **Sidebar Resources / Connectors gated by plan + presence** ([#1571](https://github.com/kagura-ai/memory-cloud/issues/1571)): the entries appear when the plan carries the feature or the workspace already owns such objects (one lazy list probe per workspace per session on lower tiers); existing objects keep working ([#1551](https://github.com/kagura-ai/memory-cloud/issues/1551)).
- `self_hosted` chat completions honour `SELF_HOSTED_MODEL_ALIASES`, retry once without `response_format` when the server rejects it, and read `SELF_HOSTED_LLM_TIMEOUT_SECONDS` ([#1569](https://github.com/kagura-ai/memory-cloud/issues/1569)). `SLEEP_LLM_PROVIDER` accepts `anthropic` and `gemini`.

### Changed
- **Context-level `use_rerank` applies when the caller omits the flag** ([#1572](https://github.com/kagura-ai/memory-cloud/issues/1572)): `RecallRequest.use_rerank` and the MCP `recall` argument are tri-state — omitted follows the context's search config; explicit `true` / `false` behaves as before. Existing contexts default to `false`, so only contexts whose owner enabled reranking change behaviour.
- **`ENABLE_RERANKING` is now a real deployment kill switch** ([#1572](https://github.com/kagura-ai/memory-cloud/issues/1572)); it was read nowhere before. Default `true` (the `.env.example` value was corrected from `false`).
- **`self_hosted` embeddings are spend-capped iff their effective price is above zero** ([#1570](https://github.com/kagura-ai/memory-cloud/issues/1570)): a priced self-hosted model gets `PLAN_<KEY>_EMBEDDING_*_CAP_USD`, the per-workspace override, the 80 % / 100 % owner mails and `QUOTA-002` on every tier; unpriced stays uncapped.
- **Unknown price renders as `—`, not `$0.00`** ([#1570](https://github.com/kagura-ai/memory-cloud/issues/1570)): migration `e80` removes the seeded `$0` rows for the five `self_hosted` embedding models (FK-referenced rows kept). Set an override at price `0` to get `$0.00` back for a truly free local model.
- **Analysis estimate and actual cost read the same pricing snapshot** ([#1570](https://github.com/kagura-ai/memory-cloud/issues/1570)): `estimated_cost_cents` (REST preview, MCP `analyze_context(dry_run=true)`) and `cost_estimated_cents` / `cost_actual_cents` are `null` when the model is unpriced instead of a hardcoded guess or a 500. An unpriced managed-lane run records `model_id = NULL` plus new `llm_provider` / `llm_model` columns (migration `e81`, [#1569](https://github.com/kagura-ai/memory-cloud/issues/1569)).
- **Reranker fail-open is visible** ([#1572](https://github.com/kagura-ai/memory-cloud/issues/1572)): a fully failed `self_hosted` scoring batch now degrades to un-reranked hybrid results with a `rerank_failed_open` warning instead of silently returning arbitrary order. New contexts write `reranker_model=rerank-2` (was `rerank-2-lite` on one path).
- Analysis VAL-001 refusal text names both routes (workspace OpenAI key, or the deployment's managed lane + `managed_llm` feature); a caller-pinned pricing `model_id` is refused (422) on the managed lane ([#1569](https://github.com/kagura-ai/memory-cloud/issues/1569)).
- The web UI reranker card shows the deployment's actual default and is disabled with a note when `features.reranking` is `false` ([#1572](https://github.com/kagura-ai/memory-cloud/issues/1572)).

### Notes
- **Migrations:** `alembic upgrade head` — `e80_1570` (delete seeded `$0` self_hosted pricing rows) and `e81_1569` (`memory_analyses.model_id` nullable, `llm_provider` / `llm_model` columns). `e81` downgrade refuses while unpriced (NULL `model_id`) runs exist.
- **New env knobs:** `LLM_PRICING_OVERRIDES`; `DEFAULT_RERANKER_PROVIDER` / `DEFAULT_USE_RERANK` / `DEFAULT_RERANKER_MODEL`; `MANAGED_LLM_PROVIDER` / `MANAGED_LLM_MODEL`; `RESOLVE_STORED_BYOK_KEYS`; `SELF_HOSTED_LLM_TIMEOUT_SECONDS`; `ENABLE_COST_DISPLAY`. `ENABLE_RERANKING` and the `RERANK_BASE_URL` / `RERANK_MODEL` / `RERANK_API_KEY` / `SELF_HOSTED_RERANK_MODEL` family are now documented. Half-configured lanes or malformed overrides refuse to start the API, naming the variable. See Deployment → *LLM & Embedding Pricing*, *Reranking*, *LLM Credentials* and *Hosted-mode UI gates*.
- **New log events:** `llm_pricing_override_applied` / `llm_pricing_overrides_synced` / `llm_pricing_overrides_sync_failed`, `rerank_failed_open`, `analysis_managed_model_unpriced`, `sleep_llm_from_managed_lane`, `byok_key_resolution_disabled`, `self_hosted_response_format_unsupported`.

## [v0.68.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.68.0) — 2026-09-19

The four-tier plan matrix ([#1547](https://github.com/kagura-ai/memory-cloud/issues/1547)).

### Added
- **XL (`promax`) tier** ([#1547](https://github.com/kagura-ai/memory-cloud/issues/1547), [#1551](https://github.com/kagura-ai/memory-cloud/issues/1551)): a fourth plan above L (`pro`). Tiers are S / M / L / XL (`free` / `basic` / `pro` / `promax`); display names remain env-overridable.
- **Owned-workspace cap by plan** ([#1550](https://github.com/kagura-ai/memory-cloud/issues/1550)): a user may own **1 / 1 / 3 / 20** workspaces on S / M / L / XL (`owned_workspace_grant`). Existing workspaces above the cap are kept — only creation is gated.
- **Daily memory-creation quota `memories_per_day`** ([#1549](https://github.com/kagura-ai/memory-cloud/issues/1549)): memories created per workspace per UTC day — **50 / 300 / 2,000 / 10,000** by default (`0` means none, not unlimited). The Redis counter fails open and is observable via structured logs ([#1556](https://github.com/kagura-ai/memory-cloud/issues/1556)).
- **`PLAN_<KEY>_FEATURES` env override** ([#1559](https://github.com/kagura-ai/memory-cloud/issues/1559)): a self-hosted deployment can re-enable `resources` / `connectors` / `public_contexts` (or any feature) on a lower tier; unknown names and invariant violations (`resources` ⊆ `public_contexts`, `secret_store` everywhere) are rejected at startup and `FEATURE_MIN_PLANS` follows the effective tiers.
- **Blocking Redis pool** ([#1556](https://github.com/kagura-ai/memory-cloud/issues/1556)): `REDIS_MAX_CONNECTIONS` (50) / `REDIS_POOL_TIMEOUT_SECONDS` (2.0); pool exhaustion no longer turns the daily quotas fail-open silently — every fail-open emits `quota_check_failed_open`.
- **Admin add-on warning** ([#1561](https://github.com/kagura-ai/memory-cloud/issues/1561)): `PUT /api/v1/admin/plans/workspaces/{id}/quotas` now returns `warnings` (e.g. `["connectors_feature_missing"]`) when `extra_connectors` seats are granted to a tier without the `connectors` feature. The grant is stored — admins may pre-grant ahead of an upgrade — but stays inert until the workspace is upgraded; the admin UI shows the warning in the add-on dialog.

### Changed
- **L (`pro`) can no longer create public contexts, resources or connectors** ([#1551](https://github.com/kagura-ai/memory-cloud/issues/1551)): `setup_resource`, new resource tokens, `setup_connector` and the private→public context transition require the `resources` / `connectors` / `public_contexts` feature, which only XL carries by default. **This blocks new creation only — existing public contexts, resource tokens and connectors on L keep working** (listed, served, editable, ingesting). The M/L token and connector caps now bound only what those plans already hold.
- **Unified feature-refusal text** ([#1561](https://github.com/kagura-ai/memory-cloud/issues/1561)): the plan-feature gates for `connectors`, `resources`, `public_contexts`, `QuotaService.check_feature_access` and the REST shared-context routes now build their FEAT-001 refusal via `FeatureNotAvailableError.for_feature`, which derives the tier name from the plan registry and always sets `details.feature`. The REST shared-context gates (create and update) answer FEAT-001 (403) with that text instead of fixed "Pro plan" wording; the update path previously answered 400. The analysis and sleep gates keep their own wording.
- **Frontend create gates and token caps come from the plan API** ([#1560](https://github.com/kagura-ai/memory-cloud/issues/1560)): `usePlanFeatures` reads the tier booleans and `GET /workspaces/{id}/plan` quotas instead of hard-coded tier names; the plan page separates "what each plan can create" from the workspace's current entitlements.
- **Legacy tier enums removed** ([#1553](https://github.com/kagura-ai/memory-cloud/issues/1553)): `UserPlan` / `RETENTION_BY_PLAN`.
- Docs swept for stale "Pro plan" creation claims — `mcp-tools`, `resource-tokens-guide`, `api-surface-1.0/mcp-input-schemas`, `connector-ingest-contract` ([#1561](https://github.com/kagura-ai/memory-cloud/issues/1561)).

### Notes
- **Billing:** the billing side must map a `promax` SKU before XL can be sold self-serve; until then XL is admin-assigned. Existing L subscriptions are not migrated automatically.
- **New env knobs:** `PLAN_PROMAX_*` (numeric limits and display name for the new tier), `PLAN_<KEY>_MEMORIES_PER_DAY` and `PLAN_<KEY>_OWNED_WORKSPACE_GRANT` for every tier key. `PLAN_<KEY>_FEATURES` overrides a tier's feature set ([#1559](https://github.com/kagura-ai/memory-cloud/issues/1559)); `REDIS_MAX_CONNECTIONS` / `REDIS_POOL_TIMEOUT_SECONDS` size the Redis pool ([#1556](https://github.com/kagura-ai/memory-cloud/issues/1556)). See [Deployment → Plan Tiers](docs/deployment.md#plan-tiers).

## [v0.50.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.50.0) — 2026-07-15

### Fixed
- **Bootstrap trusted-tier gate** ([#1293](https://github.com/kagura-ai/memory-cloud/issues/1293)): the agent bootstrap `pinned` and upcoming-time-memory lanes now apply the same trusted-only filter as the recall lane, so external/connector-origin memories can no longer establish agent behaviour at session start (OWASP LLM01/LLM03). The user-initiated standalone `load_pinned` / `recall_upcoming` tools are unchanged.
- **Per-transition agent audit rows** ([#1294](https://github.com/kagura-ai/memory-cloud/issues/1294)): a combined status + enforcement `PATCH /api/v1/agents/{id}` now emits one audit row per governed transition instead of collapsing them, so the status kill-switch and the enforcement change are each first-class in the structured audit columns. REST and MCP share one helper, closing the drift that let both surfaces carry the bug.

### Tests
- **Agent access-control E2E regression** ([#1295](https://github.com/kagura-ai/memory-cloud/issues/1295)): DB-backed end-to-end coverage for the [#1291](https://github.com/kagura-ai/memory-cloud/issues/1291) subtractive agent-binding recall gate.

## [v0.49.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.49.0) — 2026-07-15

### Added
- **Agent Registry** ([#1274](https://github.com/kagura-ai/memory-cloud/issues/1274)): workspace-scoped agent identities and lifecycle APIs/MCP tools.
- **Agent↔context bindings and agent-bound API keys** ([#1275](https://github.com/kagura-ai/memory-cloud/issues/1275)): subtractive context access policy for agent credentials.
- **Agent bootstrap** ([#1276](https://github.com/kagura-ai/memory-cloud/issues/1276)): `get_agent_bootstrap` returns the effective context envelope for an agent.
- **Agent correlation** ([#1277](https://github.com/kagura-ai/memory-cloud/issues/1277)): W3C `traceparent`/baggage parsing on REST and MCP, verified identity precedence, and correlated bootstrap envelopes. Correlation is observability only; server-side OTel span export remains a P1 non-goal.
- **Agent memory audit** ([#1278](https://github.com/kagura-ai/memory-cloud/issues/1278)): append-only, trigger-enforced `memory_access_events` storage with a fail-open independent writer, HMAC-keyed query hashes, erasure handling, and emission for bootstrap, load-pinned, feedback, recall, reference, and remember operations.

### Changed
- **Agent control-plane hardening** ([#1281](https://github.com/kagura-ai/memory-cloud/issues/1281)): confined REST memory calls to the API key's workspace, added the agent-key workspace database invariant, reset REST agent scope defensively, fixed bootstrap cap/validation/error mapping, and applied subtractive agent bindings to analysis surfaces.
- **Memory Analysis hardening** ([#1247](https://github.com/kagura-ai/memory-cloud/issues/1247)): generic MCP error envelopes, stable `(started_at, id)` keyset pagination, and accounting for failed-but-billed LLM attempts. The connection-pool item was explicitly waived pending a broader `LLMService` refactor.

### Notes
- This is an additive release with no breaking REST or MCP changes. Agent enforcement remains opt-in and defaults to `shadow`.
- Per-memory type/source binding filters remain fail-closed and accept only `null`. Audit emission for `update`/`forget`, binding deny/`would_deny` persistence, and identity-precedence contract follow-ups continue in [#1286](https://github.com/kagura-ai/memory-cloud/issues/1286).

## [v0.48.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.48.0) — 2026-07-14

### Changed
- REST and MCP batch resource ingestion now share `ResourceIngestService`, including identical UTF-8 byte-size accounting and validation behavior ([#1255](https://github.com/kagura-ai/memory-cloud/issues/1255)).
- Added implementation-ready Agent Memory & Context Control Plane design, operations, and evaluation documents ([#1258](https://github.com/kagura-ai/memory-cloud/issues/1258)–[#1264](https://github.com/kagura-ai/memory-cloud/issues/1264)). These documents define the v0.49.0 delivery plan; they did not by themselves ship the control plane.

## [v0.47.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.47.0) — 2026-07-14

### Fixed
- Hardened memory analysis runs with a configurable memory-count cap (`ANALYSIS_MAX_MEMORY_COUNT`, default 10,000), failure when more than half of cluster-label calls fail, all-or-nothing cancellation, strict BYOK enforcement, soft-deleted-context invisibility, and race-safe run creation/assignment handling ([milestone](https://github.com/kagura-ai/memory-cloud/milestone/78)).

## Historical development notes (pre-v0.16.0)

### Added
- **Embedded LanceDB vector backend — "Kagura Lite" (preview)**: a feature-flagged, in-process vector store for single-process self-hosted / CLI / desktop / edge deployments that want to avoid running a separate Qdrant server. Set `KAGURA_VECTOR_BACKEND=lance` (default `qdrant` is unchanged — the new path is a single `is None` branch in `db/qdrant.py`, zero behavior change for the server). New `db/vector_store.py` introduces a `VectorStore` Protocol + cached backend selector; `db/lance_store.py` implements `LanceVectorStore`. Japanese stays owned by the existing Sudachi pipeline (lemmas + readings + synonym/hiragana augmentation) feeding a weighted `search_text` FTS column, so recall behavior matches the Qdrant path without relying on LanceDB's native CJK tokenizer; hybrid 60/40 fusion stays in `services/search_service.py`. Isolation is a single-quote-escaped, UUID-validated SQL pre-filter (`build_lance_filter`). LanceDB is an **optional** dependency (`pip install '.[lite]'`) — the default install is unchanged. **Preview limitations**: `copy_context_points`, cross-collection GDPR `delete_user_points`, and the admin BM25-drift scroll raise `NotImplementedError` under this backend; the server (multi-process + nightly Sleep writer) deployment should stay on Qdrant since LanceDB writes are single-process. Unit-tested: backend selector + the security-critical filter/escaping builder (LanceDB-independent). **Post-merge hardening (max code-review follow-up):** semantic search now sets `.metric("cosine")` explicitly (LanceDB defaulted to L2 → wrong/negative similarity scores that broke confidence/absence detection); all writes serialized under the store lock (LanceDB is single-writer even in-process); embedding dimension tracked per-collection (multi-model variants no longer collide); date-range filters normalized to canonical UTC (offset-aware, validated); FTS + scalar indexes rebuilt lazily after the first write; importance-range consistency validated (4xx parity with Qdrant); reads/deletes on a not-yet-created collection return empty instead of erroring.
- **Slot-based owned-workspace cap (epic #674)**: User-level workspace ownership is now gated by `cap = 1 (base) + users.workspace_slot_bonus`, decoupling the cap from workspace plan tier (FREE/BASIC/PRO). The two axes are now orthogonal — `workspace.plan` controls feature limits (memory / api), `users.workspace_slot_bonus` controls how many workspaces a user may own. **Phase 1 ship breakdown:**
  - **Data model + cap rewrite** (#675 sub-A): alembic `e15_675_workspace_slot_bonus` adds `users.workspace_slot_bonus INTEGER NOT NULL DEFAULT 0` with a `>= 0` CHECK constraint. `QuotaService.check_workspace_creation_allowed` switched from highest-tier derivation to `BASE_CAP + bonus`. `PlanTier.max_owned_workspaces` field removed.
  - **Admin inc/dec UI** (#676 sub-B, supersedes #667): new `PATCH /api/v1/admin/users/{user_id}/workspace_slot_bonus` endpoint (atomic `UPDATE ... RETURNING` for race safety, DB CHECK as safety net, `BONUS-002` structured 400 for would-be-negative requests, audit log on every mutation, `reason` required only when the new cap would drop below current owned_count). The full detail route `/admin/users/{user_id}` surfaces the bonus value with `+1` / `−1` buttons.
  - **TOCTOU + enforce flip** (#677 sub-C): `QuotaService._acquire_workspace_create_lock` uses `pg_advisory_xact_lock(hashtextextended('workspace_create:' || user_id, 0))` (64-bit hash, per-user serialization, released on transaction commit, zero contention with other paths). The 64-bit `hashtextextended` was deliberately chosen over 32-bit `hashtext` to push birthday-paradox collisions from ~65k users out to ~2³² users (PR #686 loop 4 review). The `ENFORCE_WORKSPACE_CAP` flag flips warn-only → deny + `QuotaExceededError` 4xx. Production `memory.kagura-ai.com` flipped to `true` on 2026-05-17 (rollback runbook: `docs/ops/workspace-cap-rollback.md`).
  - **Cap visibility on admin list** (#695): `/admin/users` list response gains `owned_count` / `workspace_slot_bonus` / `base_cap` / `cap` per user (additive — mirrors `WorkspaceSummary` field names from the detail endpoint, so client code and mental models stay consistent across closely-related admin endpoints). The list page renders a new "Cap" column with at-cap emphasis and a bonus-inclusive tooltip.
  - **Tiers docs update** (#664): admin `/plans` "Tiers" tab no longer surfaces a `max_owned_workspaces` column (the value was removed from `PlanTier`).

  **Migration safety**: the grandfather migration uses `EXCLUSIVE TABLE LOCK` on `users` for the brief window of `UPDATE users SET workspace_slot_bonus = GREATEST(0, owned_count - 1)` — existing PRO users with N owned workspaces keep their effective cap (bonus = N − 1). **Idempotent**: re-running the migration converges to the same value (`GREATEST(0, count - 1)`).

  **Self-hosted operators**: `terraform/single-server/.env.prod.example` documents `ENFORCE_WORKSPACE_CAP=true` as the recommended default once log-only observation confirms no false-positive denials. The rollback runbook documents service-name-explicit `--force-recreate` recovery without redeploy.

  **Phase 2 follow-up (not in this release)**: Stripe "workspace slot" product / webhook-driven bonus increment / dangling-workspace handling. The Phase 1 model is intentionally local-only; the Phase 2 design will upgrade `workspace_slot_bonus` from a raw column to a SUM cache backed by `UserSlotPurchase` rows so the addon pattern (#485 storage_limit etc.) carries through cleanly.

### Documentation
- **R2 ChecksumSHA256 binding default-flip criteria documented** (#577): Extended `docs/ops/r2-checksum-binding-rollout.md` with §10 "Default flip & flag removal criteria" that codifies the upstream-maintainer-facing conditions for flipping `R2_CHECKSUM_BINDING_ENABLED` default from `false` to `true` (acceptance #2) and for eventually removing the flag entirely (acceptance #3). Criteria are structured across three observability axes: [A] upstream SDK availability + PyPI age (always observable), [B] hosted SDK-distribution telemetry on `memory.kagura-ai.com` covering BOTH REST `/api/v1/files/reserve` and MCP `init_file_upload`, and [C] self-hosted release-window heuristic on inbound 403 bug reports. [B] is **gated on a User-Agent capture prerequisite** that is not yet implemented — both REST and MCP currently drop `User-Agent` (§9 Future Work #1), so §10.1 [B] is documented as not-yet-evaluable until that capture lands and a 7-day observation window passes. The §10 criteria are distinct from §3 Step 3, which targets individual self-hosted operators making their own flip decisions. Also resolved the SDK version placeholder that was deferred during the #556 rollout: `kagura-memory-python-sdk v0.14.0` shipped 2026-05-11 (kagura-ai/kagura-memory-python-sdk#97), so the runbook (§3 Step 2, §4.2 example output, §8 Q3, §保守注記) and `backend/src/config/settings.py:r2_checksum_binding_enabled` docstring now reference `>= 0.14.0` instead of the original `>= 0.4.0` placeholder. **No backend behavior change** — flag default remains `false`. Acceptance #2 (flip the default) and #3 (remove the flag) remain tracked under #577 for future PRs once §10.1 / §10.2 criteria are met.

## [v0.16.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.16.0) — 2026-05-13

Pre-1.0 Hardening release: SQLAlchemy 2.0 `Mapped[T]` migration (PR-A/B/C of #370), domain-exception refactor across PermissionService / SystemAdminService / handle_update_context (#401, #603, #604), DCR scope narrowing to exclude `memory:admin` (#608), zero-base-tier defense-in-depth across all `effective_*_limit` paths (#569), CI guards for ORM↔migration drift (#587) and `Mapped[T]` (#370), alembic head merge fix for safe production rollout (#632). 20 issues closed in this milestone.

### Added
- **Body sha256 binding on presigned PUT (Phase 1.5)** (#556): `R2Storage.generate_presigned_put` now plumbs the reserved `sha256` through to R2's S3 Object Integrity feature (`ChecksumSHA256` param + `x-amz-checksum-sha256` SigV4 signing). When R2 receives bytes whose actual sha256 differs from the value signed in the URL, it rejects with HTTP 400 BadDigest before the bytes are persisted — closing the per-workspace dedup-poisoning gap that Copilot flagged 4× during PR #551 review of #485 Phase 1. The original `generate_presigned_post` proposal in #556 turned out to be non-viable on R2 (returns HTTP 501 NotImplemented for any presigned POST upload); the live integration test `backend/tests/integration/test_r2_live.py` documents that POST limitation in its module docstring and exercises the working PUT mechanism end-to-end. The integrity gate is wired but **DISABLED by default** via `R2_CHECKSUM_BINDING_ENABLED=false`, because enabling the binding makes the SDK's `x-amz-checksum-sha256` request header **required** (older deployed SDKs without it fail with HTTP 403 SignatureDoesNotMatch). **Rollout sequence for self-hosted operators:**
  1. Deploy this backend release with `R2_CHECKSUM_BINDING_ENABLED=false` (default — no behavior change vs. Phase 1).
  2. Update `kagura-memory-python-sdk` to a release that sends the `x-amz-checksum-sha256` header on PUT.
  3. Wait for SDK adoption across tenants — uploads from older SDKs will continue working in the unflipped backend.
  4. Set `R2_CHECKSUM_BINDING_ENABLED=true` to activate the integrity gate.

  The next-major release plans to default the flag true; a follow-up will remove the flag once the SDK floor is widely deployed.
- **Tier-based quota for sleep-enabled contexts** (#560): Sleep Maintenance is now a PRO-only feature with a per-workspace cap on how many contexts can have `sleep_mode != "skip"`. Effective limit = plan tier base + addon: FREE/BASIC = `0 + 0 = 0` (Sleep Maintenance unavailable), PRO = `3 + addon_sleep_contexts_bonus`. The new `extra_sleep_contexts` addon (Stripe SKU is Phase 2 follow-up; the type is wired through `AddonCalculatorService`, `WorkspaceAddon.addon_type` CHECK constraint, and the Pydantic surface so it can sell on day 1). Migration `e06_560` adds `workspaces.addon_sleep_contexts_bonus` and force-skips contexts with `sleep_mode != "skip"` on FREE/BASIC workspaces (one-way; PRO is grandfathered — see `### Notes`). Quota check in `ContextService.update_context` uses an "increase-only" rule: only blocks `skip → full/edges_only` transitions, so PRO workspaces above the limit can naturally taper down by switching contexts back to `skip`. Helper acquires `SELECT ... FOR UPDATE` on the workspace row to serialize concurrent toggles. **API contract**: rejection splits by case — `FeatureNotAvailableError` (HTTP 403, `error_code=FEAT-001`, `feature=sleep_mode`) for FREE/BASIC where the feature is plan-gated, and `QuotaExceededError` (HTTP 429, `error_code=QUOTA-001`, `quota_type=sleep_enabled_contexts`) for PRO at-or-above the cap. Frontend disables `full`/`edges_only` SelectItems in SettingsTabPanel when no headroom is left, and adds a usage card to the workspace dashboard. Motivation: Sleep Maintenance triggers LLM calls per phase, so cost-bearing access needs to track tier/plan. Public Beta launch can size LLM spend predictably.
- **Interactive graph view** (#435): Single-click on a graph node opens `MemoryDetailDialog` (deep-linked via `?memoryId=`, shared with the Memories tab so cross-tab navigation preserves the open memory). Drag-to-reposition is correctly discriminated from click using a 5px squared threshold inside d3-drag's start/end events. Edge click renders a floating metadata overlay (source title, target title, type, weight, optional `created_at` and `confidence`) anchored to the click point; dismissible on Esc / outside-click. Keyboard parity (WCAG 2.1.1): focused nodes activate via Enter/Space. Edge hit area uses a transparent stroke-width=10 line stacked on top of the 1px visual line so users don't need to land precisely on the stroke.
- **Admin-configurable signup gate (Phase 1)** (#358): New `/admin/signup-gate` admin page controls who can register on self-hosted and public instances. Default off preserves existing OSS behavior (`ALLOW_REGISTRATION` env flag continues to control signup). When enabled with mode=`manual`, only GitHub users on the admin-managed allowlist may register; attempts from non-allowlisted accounts redirect to `/signup-blocked`. Matching is on the immutable numeric `github_user_id` (renames don't break the match). Added `signup_gate_config` (singleton) + `signup_allowlist` tables via migration `b04`.

### Changed
- **Default `sleep_mode` for new contexts is now `skip`** (#558): `Context.sleep_mode` `server_default` was flipped from `"full"` to `"skip"` so newly created contexts make no Sleep Maintenance LLM calls until the owner opts in via Settings (#504). The migration is metadata-only (`ALTER COLUMN ... SET DEFAULT`) — existing contexts and rows are unchanged. The Sleep orchestrator's lookup fallback (when `context_id` is missing or DB lookup fails) was also flipped to `"skip"` so an unidentifiable context doesn't burn LLM tokens. The motivation is cost predictability ahead of Public Beta: an opt-in model lets operators reason about LLM spend as signup volume grows.
- **Erasure-request `confirm_token` is now nullable for OAuth users** (#469): `POST /api/v1/me/account/erasure-request` 201 response previously always populated `confirm_token: string`. With this change, OAuth-auth users get `confirm_token: null` (the raw token is delivered via email instead, narrowing the in-band disclosure surface). Password-auth users continue to receive the raw token in-band as the second factor for password re-prompt at confirm time. SDK / client consumers reading `confirm_token` MUST now treat it as `string | null`. Token TTL is 1 hour regardless of delivery channel, so existing mid-flow requests at deploy time will expire within the window. Production `EMAIL_PROVIDER=resend` flip is gated separately on Privacy Policy sub-processor disclosure (#480) and OAuth IdP-side email sync (#481) — code merge of #469 does not flip the channel.
- **Shared-context memory visibility** (#435 follow-up): `GET /api/v1/memory/list?context_id=…` now mirrors the existing graph endpoint pattern (`api/routes/graph.py:159`) — for **shared (non-private) contexts**, every workspace member with read access sees every memory regardless of original creator; for **private contexts**, only the creator continues to see their own memories. Previously the endpoint hard-applied `Memory.user_id == caller`, which meant admin / non-creator team members saw an empty list even after `PermissionService` granted read access. The unscoped "list my memories" path (no `context_id`) is unchanged. Self-hosted operators on team plans should be aware that members of shared contexts will now see each other's memories via the Memories tab UI (the underlying graph view already exposed this).
- **Context detail title icon** (#435 follow-up): The privacy glyph next to the context title now matches the contexts list page — Globe (purple) for public, Lock (blue) for private, Users (green) for shared. Previously the detail page only distinguished private vs non-private, missing the public case and using gray/blue colors.

### Fixed
- **Dev-mode 500 with Next.js 16 + Turbopack** (frontend): `output: "standalone"` in `next.config.ts` is now gated on `NODE_ENV === "production"`. With `next dev` (Turbopack), the unconditional setting caused every request to 500 with `ENOENT: .next/dev/server/app/page/build-manifest.json`. Production Docker builds set `NODE_ENV=production` explicitly in the builder stage so the standalone bundle is still emitted reliably.

### Notes
- **Breaking change for FREE/BASIC users (#560)**: contexts that previously had `sleep_mode = "full"` or `"edges_only"` on FREE/BASIC workspaces are force-skipped during the `e06_560` data migration. These users were never charged for the LLM usage but were generating platform-side cost; the migration brings their behavior in line with the new tier policy. Operator should call this out in the v0.16.0 release notes and consider an in-app banner for FREE/BASIC accounts on first login post-deploy. PRO over-limit users are grandfathered (see `### Added`).
- **`extra_sleep_contexts` addon Stripe SKU**: not yet sold. The addon type is fully wired (`ADDON_UNIT_VALUES`, `WorkspaceAddon` CHECK constraint, `AddonCalculatorService`, `Workspace.addon_sleep_contexts_bonus`, `effective_sleep_enabled_contexts_limit`), so once the Stripe SKU is created and `WorkspaceAddon` rows of this type start appearing, the existing recalculator picks them up. Tracked as a Phase 2 follow-up.
- **Phase 2 follow-up**: GitHub Sponsors sync (`sponsorshipsAsMaintainer` GraphQL cron + `POST /webhooks/github/sponsors` HMAC receiver) and the 6 sponsorship-action state machine are deliberately not yet implemented. Mode selector exposes `github_sponsors` / `both` as disabled options; the PUT update schema is narrowed to `Literal["manual"]` so the backend returns `422` for direct-API calls that set those modes. Table schema already reserves all Sponsors columns so Phase 2 lands without a follow-up migration.
- **Privacy Policy dependency**: before enabling this gate on any hosted deployment, Privacy Policy must document allowlist retention + revocation flow. Tracked under #359 (`docs(legal): Privacy Policy / ToS / Cookie Policy draft`) — ship together per the v0.14.0 Access + Legal bundle rule.

## [v0.12.3](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.12.3) — 2026-04-19

Consolidated release rolling up the planned v0.12.1, v0.12.2, and v0.12.3 content into a single tag. Full notes on the [GitHub Release page](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.12.3).

## [v0.12.0](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.12.0) — 2026-04-16

### Highlights

The Resource Foundation refactor (epic #321) normalizes the resource subsystem around a new `resources` entity with UUID primary keys, enforces cross-tenant isolation at both the database and API layers, and maintains full backward compatibility for all external API contracts (REST and MCP). See [`docs/resource-foundation-migration.md`](docs/resource-foundation-migration.md) for the self-hosted migration guide.

### Security
- **Critical (OWASP A01 / CWE-639)**: Fixed cross-tenant memory injection via Resource Ingest API (#322, parent epic #321). All self-hosted deployments with Resource Ingest enabled must upgrade. See `SECURITY.md` for the advisory and upgrade steps.

### Added
- **Resources entity** (#323): New `resources` table (UUID PK, workspace FK) as the authoritative source of truth for resource identity. Satellite tables (`resource_events`, `resource_schemas`, `indexer_state`, `resource_tokens`) gain `resource_pk` UUID FK columns with backfill from existing data.
- **API compatibility shim** (#326): `PermissionService.resolve_resource_by_slug()` translates incoming `resource_id` slugs to internal UUIDs with workspace boundary enforcement. External API URLs remain unchanged.
- **Resource Indexer API** (#326): `GET /api/v1/resources/{resource_id}/indexer-status` endpoint for monitoring indexer progress per resource.

### Fixed
- **Resource indexer per-context EmbeddingService selection** (#338): After #334 routed per-context Qdrant collections, `_apply_upsert` still called `EmbeddingService` with the global default model, so contexts using e.g. `qwen3-embedding:8b` (4096 dim) had upserts rejected on dim mismatch against the 4096-dim collection. The per-batch routing helper now resolves `(collection_name, embedding_service)` from a single `ContextSearchConfig` fetch (`_resolve_routing_for_context`) so both values come from the same config. Falls back to the legacy `kagura_memories` collection + the indexer's default `EmbeddingService` when no `ContextSearchConfig` row exists — matching `memory_service` exactly, so both services keep reading/writing the same collection for legacy contexts. Operators overriding `settings.embedding_model` must create a `ContextSearchConfig` row per context to opt into the per-context routing path.
- **Resource indexer per-context collection routing** (#334): `resource_indexer` was hardcoding the legacy `kagura_memories` (512-dim) collection at every Qdrant call site, so any context configured with a non-default embedding model (e.g. `qwen3-embedding:8b` → 4096 dim) would have its upserts rejected by Qdrant on dim mismatch. The indexer now resolves the per-context collection via `get_collection_name(model, dimensions)` from `ContextSearchConfig`, mirroring the existing `memory_service` pattern. Falls back to legacy `kagura_memories` when no `ContextSearchConfig` row exists. Layer C (per-context `EmbeddingService` selection) is addressed by #338.
- **Resource indexer Qdrant upsert** (#324): Resource ingest events were silently failing at the Qdrant write path with `"Not existing vector name error"` because the indexer upserted anonymous vectors into a named-vector collection (`dense` + sparse `bm25`). Fixed by switching to `PointStruct(vector={"dense": embedding})` and unmasked a latent `Memory(collection_name=...)` kwarg error left over from the Single Collection Migration. See `docs/ops/resource-indexer-backfill.md` for re-queue procedure for stuck `indexer_state` rows.

### Database
- Added migration `a96`: global partial UNIQUE index `ux_contexts_resource_id_active` on `contexts(resource_id)` for active rows. Zero-downtime (`CREATE INDEX CONCURRENTLY`). Aborts if pre-existing cross-workspace `resource_id` collisions are detected — operators must resolve duplicates before upgrading.
- Added migration `a97`: `resources` entity table, `resource_pk` UUID FK on satellite tables with backfill, `resource_tokens.workspace_id` shadow FK, and three partial UNIQUE indexes. Includes pre-flight audits for orphan rows and cross-workspace ambiguity.

### Documentation
- Added `docs/resource-foundation-migration.md`: self-hosted migration guide with prerequisites, step-by-step instructions, rollback procedures, and API compatibility details.
- Updated `SECURITY.md`: strengthened upgrade recommendation for self-hosted operators.

### Superseded issues
- #318 (resource_events IntegrityError dispatch) — fixed in PR #346
- #319 (resource indexer per-context collection routing) — fixed in PR #342
- #320 (resource indexer per-context EmbeddingService selection) — fixed in PR #342

## [v0.11.1](https://github.com/kagura-ai/memory-cloud/releases/tag/v0.11.1) — 2026-04-12
